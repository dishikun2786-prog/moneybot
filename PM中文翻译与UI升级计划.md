# PM 中文翻译 + 搜索交互 + 商业级 UI 升级计划（R11）

> 审查日期：2026-09-23 ｜ 数据源实测：Polymarket gamma `language=zh` 参数**无效**（返回英文原文）→ 确认无中文 API，需 DeepSeek 翻译。

---

## 一、P1：PM 搜索/展示内容自动翻译中文（DeepSeek API）

### 现状
- pm_tokens.json 12596 条 token 的 title/question 全部英文（如 "Dota 2: Team Nemesis vs Conventus Stellarum (BO3)..."）
- 前端 PM 卡片/搜索/下单表单全部直接展示英文
- 服务器已有 DeepSeek key：`~/polymarket/.ai_secrets.json`（AI 策略助手在用，600 权限）

### 方案
**1. 翻译服务 `tools/pm_translate.py`（后台批处理，不在请求路径上）**
- 输入：pm_tokens.json，按 `key`（事件|市场）聚合去重出唯一标题集（预计 3000-5000 条）
- 输出：`logs/pm_zh.json` — `{key: {title_zh, question_zh}}`，原子写
- 调用：DeepSeek `deepseek-chat`（复用 ai_client 同款 key/端点），每批 30 条：
  - 系统提示：「你是体育/政治/加密事件的翻译助手，翻译成简体中文，保留队伍名缩写（NBA/MMA/Dota），简洁 ≤20 字」
  - 返回 JSON 数组严格对齐
- **增量**：已翻译的 key 跳过；pm-clob 目录刷新后新增标题才翻译（每日 03:05 cron 兜底全量对账）
- 限速：批间 0.5s；失败重试 2 次；某批失败不阻塞整体
- 成本：~5000 条 × ~20 输出 token ≈ 10 万 token ≈ ¥0.1-0.3，商业可行

**2. API 透传**：`/api/pm/tokens` 返回项加 `title_zh/question_zh`（读 pm_zh.json 查表，缺则回退英文）；`q` 搜索同时匹配中文（title_zh 包含）

**3. 前端**
- PM 卡片：标题大字显示中文，英文原文小字灰显（保留做参考）
- 下单表单市场下拉：中文在前
- 搜索结果即时中文匹配

### 验收
- 服务器实测：pm_zh.json 生成 ≥95% 覆盖率；API 返回 title_zh；中文搜索「比特币」命中
- 前端：PM 屏 90%+ 卡片显示中文标题

---

## 二、P2：搜索展示交互修复（取消/清除/不遮挡）

### 现状问题
- pm-q 搜索框输入后**没有清除按钮**——用户无法一键取消搜索、恢复完整列表
- 搜索结果过滤后列表内容变化，但无「取消搜索」提示，用户困惑
- 下单表单 `pm-key` 是原生 select，移动端点开弹系统选择器，体验割裂

### 方案
1. **搜索框组件化**：
   - 右侧 × 按钮（有输入时显示）：一键清空 → 恢复全列表 + 键盘收起（blur）
   - 搜索时列表顶部显示「🔍 搜索结果 N 条 · 点击清除」提示条（点击即清空）
2. **下单表单市场选择器重做**（弃原生 select）：
   - 点「市场」字段 → 底部弹层（复用现有 sheet 机制：遮罩 + 上滑面板）
   - 弹层内：搜索框 + 已选市场高亮 + 列表滚动 + **取消按钮** + 点遮罩关闭
   - 点击卡片 pmPickKey 后：自动关闭弹层 + 滚到下单区 + 填充选中
3. **防遮挡**：搜索结果/弹层均为可关闭浮层，任何浮层出现时其余内容可点击遮罩关闭

### 验收
- 搜索→点×→全列表恢复；搜索词清空后无残留过滤
- 市场选择弹层：开→选→关 全路径；点遮罩关闭；不遮挡底栏

---

## 三、P3：商业级 UI 交互升级（分批落地）

### 设计原则（对标 Binance/OKX 移动端 + Stripe 质感）
1. **8pt 栅格 + 统一设计 token**：间距 4/8/12/16/24；圆角 8/12/16；统一在 CSS 变量层
2. **卡片层次**：三级阴影（rest/hover/pressed）+ 描边；可点卡片 :active 缩放 0.98
3. **动效体系**（60fps，仅 transform/opacity）：
   - 页面切换：slide-fade 200ms
   - 列表项入场：stagger 30ms（首屏骨架后数据到达时）
   - 价格变化：涨绿跌红 vflash 500ms（已有，强化）
   - 按钮 loading：spinner + 防重复点击
4. **底部导航**：活跃项指示条（2px 渐变小条 + 图标/文字变色）+ 切换微动效
5. **表单**：聚焦光晕（box-shadow accent 40%）、错误态红色描边+提示、标签上浮
6. **空态/错态统一**：emoji 插画 + 白话文案 + 操作按钮（如「重新加载」）
7. **触感**：所有可点元素 ≥44px 触区；危险操作（平仓/打款）红色 + 确认弹层

### 落地批次
| 批 | 范围 | 交付 |
|---|---|---|
| P3-a | CSS token 层重构（颜色/圆角/阴影/动效变量） | 全站视觉统一基线 |
| P3-b | 底部导航 + 页面切换动效 + 卡片点击反馈 | 核心导航体验 |
| P3-c | 表单/按钮/空态错态 + 触区 | 组件级打磨 |
| P3-d | 交易屏/详情屏专业行情卡片（K线+盘口+下单一体视觉） | 核心交易页 |

---

## 四、实施顺序与风险

1. **P1（翻译）** → P2（搜索交互） → P3-a/b → P3-c/d
2. 风险：
   - DeepSeek 偶发慢：批处理后台执行，不阻塞请求路径；缺翻译回退英文（优雅降级）
   - 翻译质量：体育队伍名保留英文缩写；加密/政治事件确保术语准确
   - UI 改动回归：每次 P3 批次后跑 smoke 冒烟 + 全量测试

## 五、工作量预估
- P1：后端翻译服务 + API + 前端展示（1 个文件新增 + 2 处修改）
- P2：m.html 搜索组件 + 市场选择弹层（前端为主）
- P3：4 批 CSS/组件重构（每批独立可回归）
