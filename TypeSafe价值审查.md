# TypeSafe AI 在现有技术栈中的价值审查

> 2026-09-22 · 工程视角审查，不含营销滤镜。审查依据：官方文档、GitHub、三次实测调用、现有技术栈代码。

---

## 0. 结论（先给答案）

| 项 | 裁决 |
|---|---|
| TypeSafe 整体价值 | **有，但范围远窄于原计划**——从「L2 决策层核心」**降级为「研究工具 + 事件解释实验台」** |
| 集成点1（新闻→信号） | ⚠️ **条件价值**：唯一的合理用途；必须通过「回测证明事件edge存在」+「bake-off 实测胜出」两道门才允许进资金路径 |
| 集成点2（信号置信门控） | ❌ **负价值**：确定性检查更快、免费、可回放、无外部依赖 |
| 集成点3（regime 授权） | ❌ **负价值**：分位数统计 μs 级解决，LLM 无增量信息 |
| L3 研究/盘后解释 | 🟡 中性：可用但无差异化（通用 LLM 亦可） |
| 沉没成本 | ≈0（API key 无月费），不存在「必须用」的压力 |

**一句话**：TypeSafe 擅长的是**文本判断工作流**（客服单、理赔单、风控单），官方用例地图里**没有交易/量化场景**；把它塞进确定性定价+统计风控的量化栈，能落地的只有「新闻→结构化信号」这一件事，且必须与免费基线公平竞赛。

---

## 1. 它是什么 / 不是什么（实测事实）

**是**：一个「窄决策 + 校准概率 + 置信度」的结构化输出服务。输入 state（文本/JSON）+ 类型化问题（noul/choice/score），输出 P(yes)、选项概率分布、confidence。延迟实测：首包 425–450ms（TLS 243ms 是大头），keep-alive 稳态 ~250ms。模型 jev-1.13.0。

**不是**：
- 不是交易/预测模型——官方 use-case-map 覆盖的是保险理赔、金融犯罪、电商、风险评估（全是**文档/沟通文本判断**），零交易用例
- 不是快过 250ms 的「System One 直觉」——250ms 意味着只能参与秒级以上的决策
- 校准声明未经验证——「calibrated probabilities」是卖点，但没有任何第三方/我们自己的实测背书

---

## 2. 逐集成点价值分析

### 2.1 新闻→结构化信号（有条件价值 ⚠️）

**为什么可能是唯一有价值的地方**：非结构化新闻文本 → {市场, 方向, 幅度, 概率} 是确定性代码的盲区；事件错价窗口是分钟级，250ms 延迟可接受；概率输出理论上可做仓位大小（Kelly 式）。

**竞争对手**：
| 方案 | 延迟 | 成本 | 备注 |
|---|---|---|---|
| Jev (TypeSafe) | ~250ms | 低 | 校准概率 + confidence 双轴 |
| DeepSeek JSON mode | ~1-3s | 极低 | **用户已有 DeepSeek 接入**，结构化输出成熟 |
| FinBERT + 规则 | ~10ms | 0 | **已在全球情报中心栈内**（情绪判断，但无结构化的市场/幅度） |

**TypeSafe 的真实差异点**：①校准概率（若为真，可喂仓位计算）②速度比通用大模型快一个量级。

**准入条件（两道门）**：
1. **回测门**：T2.4 回测必须先证明「事件驱动 edge」存在且盈利（目前零证据——这是最大的未知数）
2. **bake-off 门**：在标注集上，Jev 必须在「概率校准（ECE）」或「方向准确率」上**显著优于** DeepSeek JSON / FinBERT 基线，才值得引入外部依赖

**风险**：若校准其实不准，把概率喂给仓位计算反而有害（比不用更差）。领域迁移风险：模型训练于客服/理赔文本，金融新闻语义是否同样适用未验证。

### 2.2 信号置信门控（负价值 ❌）

原设计：L1 候选信号（公允价偏离 >2σ 等）→ Jev 二审「是不是真错价」。

**问题**：信号本身是**确定性数学**（公允价偏离、碰价价差），门控要查的其实是：
- 数据新鲜度（报价时间戳 < N 秒）→ 确定性
- 流动性（盘口量 ≥ 100 股）→ 确定性
- 价差/网格（tick 是否正常）→ 确定性
- 限频余量 → 确定性

这些检查 **μs 级、免费、完全可回放、无外部依赖**。引入一个 250ms 的外部模型做二审：
- 延迟 +250ms（错过部分窗口）
- 非确定性（同一输入可能不同输出，不可审计回放）
- 把外部 API 变成风控路径的单点依赖
- LLM 的「判断」在无标注数据上无法验证对错

**裁决**：改为**确定性 gatekeeper**（新鲜度/流动性/价差/限频四查）。这正是官方哲学「code in control」——比官方自己建议的更彻底。

### 2.3 波动率 regime 授权（负价值 ❌）

原设计：每 15 分钟让 Jev 给波动率 regime 打分（0-3 档）+ 「是否降敞口」。

**问题**：regime 是纯统计问题：
- 已实现波动率分位数（过去 90 天 vol 分布中的位置）→ μs 级
- IV 分位数（Deribit IV 表的分位）→ μs 级
- 映射表（分位 → ATR 系数）→ 可回测调参

LLM 打分：250ms 延迟、非确定性、**无法用历史数据回放验证**（模型会变，回测结论作废）。确定性分位数还免费、零依赖。

**裁决**：vol/IV 分位数 + 映射表。TypeSafe 出局。

### 2.4 L3 研究/盘后解释（中性 🟡）

「为什么这笔亏了」「今日异常综述」——可用，但通用 LLM（已有 DeepSeek）一样做，TypeSafe 的结构化输出在此无差异化。**用哪家都行，不是 TypeSafe 的价值证明。**

---

## 3. 量化对比总表

| 用途 | TypeSafe | 最佳替代 | 替代的延迟/成本/可回放性 | 裁决 |
|---|---|---|---|---|
| 新闻→信号 | ~250ms/低费 | DeepSeek JSON 或 FinBERT+规则 | 1-3s 或 10ms；都需标注集评估 | ⚠️ 条件价值，bake-off 定 |
| 信号门控 | 250ms | 确定性四查 | μs / 0 / 完全可回放 | ❌ 负价值 |
| regime 授权 | 250ms | vol/IV 分位数 | μs / 0 / 完全可回放 | ❌ 负价值 |
| 盘后解释 | 秒级 | 通用 LLM | 秒级 / 低 | 🟡 无差异化 |

---

## 4. Bake-off 实验设计（TypeSafe 能否进资金路径的唯一判据）

**数据集**：收集 50 条历史新闻事件（含标题+摘要+时间），标注：
- 人工标注 {影响市场, 方向, 幅度档}
- **Ground truth**：事件后 15 分钟该市场真实价格变动（胜于人工标注的客观基准）

**参赛者**：Jev（现模块）vs DeepSeek JSON mode vs FinBERT+规则（栈内现成）

**指标**：
| 指标 | 说明 | 门槛 |
|---|---|---|
| 方向准确率 | 预测方向 vs 实际变动 | Jev 需显著高于基线 |
| **ECE（校准误差）** | 概率与真实频率的偏差（reliability diagram） | Jev 若 ECE>0.1，其「校准概率」卖点证伪 |
| 延迟 | 端到端 | 全部满足 <2s 即可 |
| 成本 | 每千条事件 | 忽略不计 |

**执行**：作为研究任务（不进资金路径），脚本化跑完出一张表。
**准入规则**：只有 Jev 在 ECE 或准确率上显著胜出，才允许在资金路径中用其概率输出；否则事件信号管道用获胜的基线（或干脆不用 LLM）。

---

## 5. 修订后的定位与影响

**保留**：
- `~/polymarket/typesafe_decision.py` 作为**研究工具**（bake-off、事件实验、盘后分析辅助）
- API key（零持有成本）

**修改（已同步到修复计划）**：
1. 计划架构「L2 决策层（TypeSafe Jev）」→「L2 确定性门控为主；TypeSafe 仅事件解释、条件准入」
2. T2.3（信号门控/regime 授权）改为**确定性实现**，移除 TypeSafe
3. T2.2（新闻事件管道）加两道前置门：回测证据 + bake-off 胜出
4. 新增 T2.5：bake-off 研究实验
5. 里程碑 M2 同步调整

**净效果**：
- 风控/门控路径：**移除外部依赖**（更稳、更快、可回放）✅
- 研究路径：保留实验能力，让数据说话 ✅
- 事件交易：先证 edge、再比模型——不赌单一供应商 ✅
