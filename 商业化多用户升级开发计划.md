# 商业化升级开发计划：多用户体系 + 总管理后台 + 密钥托管 + 实盘交易

> 2026-09-22 · 现状：单用户 FastAPI + 会话 cookie + 全局纸面状态文件 + 10 个 systemd 引擎
> 目标：多用户 SaaS——注册(账号+密码+验证码)、总管理后台、用户全权限模拟交易、
> 管理端给每个用户绑定 Bybit/Polymarket 密钥进行实盘对冲套利

## 一、商业化定位与模式

| 层级 | 用户 | 能力 | 计费 |
|---|---|---|---|
| 免费版 | 注册用户 | 模拟对冲套利全功能（共享中心实时行情数据，独立纸面账户） | 免费 |
| 专业版 | 订阅用户 | 免费版全部 + AI 策略助手 + 盘口微结构 + 参数全开 | $19.9/月 |
| 实盘版 | 认证用户 | 专业版全部 + 绑定自有 Bybit/PM 密钥实盘交易 + 实盘风控 | $49.9/月 或 交易量分成 |

**核心卖点**：中心化实时行情与信号数据（已建：100ms 行情/盘口/微结构/波段评分）→ 用户免自建数据管道，登录即用。
**关键合规原则**：实盘采用「用户自持密钥」双模式——① 托管模式（加密存储，AES-GCM）② BYOK（密钥仅存用户浏览器，下单时短时使用）。PM 地理限制与风险披露必须显著提示。

## 二、技术架构（商业化改造全景）

```
┌────────────────────────── 客户端 ──────────────────────────┐
│ 登录/注册(验证码) → 用户交易室(全权限) → 我的账户(密钥绑定)    │
└───────────────┬──────────────────────────┬────────────────┘
                │ HTTPS (已有8443)          │
┌───────────────▼──────────────────────────▼────────────────┐
│            FastAPI 应用层 (多租户改造)                      │
│  认证中心: JWT会话+角色 | 验证码(图形+限流) | 密码argon2哈希   │
│  用户域:  每用户独立纸面状态/参数/引擎上下文 (共享行情只读)     │
│  管理域:  用户CRUD/套餐/风控参数/审计/密钥管理                │
│  交易域:  纸面(现状引擎多实例化) + 实盘(新增)                 │
└───────┬──────────────────────────────────────────┬─────────┘
        │                                          │
┌───────▼─────────┐                    ┌───────────▼─────────┐
│ 数据层           │                    │ 密钥保险库            │
│ PostgreSQL/SQLite│                    │ AES-256-GCM 加密      │
│ users/user_keys/ │                    │ 主密钥=环境变量/KMS    │
│ user_states/     │                    │ 永不落日志/不返回明文  │
│ audit/plans      │                    │ 解密仅在下单瞬间(内存) │
└──────────────────┘                    └─────────────────────┘
        共享行情中心 (已有, 只读): bridge/盘口/微结构/DuckDB → 所有用户
        实盘执行层 (新增): Bybit v5 下单API + PM CLOB API + 对账 + 风控
```

### 2.1 关键架构决策

| 决策 | 方案 | 理由 |
|---|---|---|
| 认证 | JWT(HttpOnly+Secure cookie) + 角色声明(admin/user) | 无状态、可横向扩展 |
| 密码 | argon2id 哈希 + 盐 | 抗 GPU 爆破 |
| 验证码 | 图形验证码(Pillow, 服务端会话绑定) + 登录失败限流(5次/15min锁定) + 可选邮箱码 | 防机器注册/撞库 |
| 多租户状态 | 每用户独立目录 `data/users/<uid>/{carry_state,paper_state,swing_cycle,...}` + 独立 strategy_params | 引擎逻辑零改动，状态物理隔离 |
| 引擎多租户 | 每用户一个轻量引擎上下文（60s tick 复用同一份行情数据）；免费用户限并发/限功能 | 行情管道共享=成本可控 |
| 密钥存储 | AES-256-GCM；主密钥仅存服务器环境变量(非git)；解密仅在下单瞬间 | 托管模式安全底线 |
| 实盘引擎 | 独立 live_engine（复用策略信号 → Bybit /v5/order/create、PM CLOB 下单），含重试/幂等/对账 | 与纸面引擎完全隔离 |
| 审计 | 全操作日志(登录/交易/改参/密钥操作, 含IP+UA) | 商业化合规 |
| 迁移 | 现有单用户数据 → admin 账户(uid=1) 完整继承 | 平滑升级 |

## 三、功能配置清单

### 3.1 用户端（注册用户全权限）

| 模块 | 功能 |
|---|---|
| 注册/登录 | 账号+密码+图形验证码；登录失败限流锁定；找回密码(邮箱) |
| 交易室 | 现有全部功能（K线/盘口/持仓/手动交易/托管手动双模式/参数编辑/AI助手/循环策略）——数据来自中心行情 |
| 模拟账户 | 独立 $100 纸面账户、独立参数、独立模式开关、盈亏看板 |
| 我的账户 | 改密、绑定/解绑 Bybit API(密钥+Secret)、绑定 PM(钱包私钥或Relayer Key)、连通性测试、套餐/续费 |
| 实盘开关 | 实盘模式下同一交易室界面，订单路由到真实交易所，显著「实盘」标识 |

### 3.2 总管理后台（admin）

| 模块 | 功能 |
|---|---|
| 用户管理 | 列表/搜索/状态(启用/禁用/锁定)/删除/重置密码 |
| 套餐管理 | 套餐定义、用户订阅、到期/续费、账单记录 |
| 密钥管理 | 每用户密钥绑定状态/连通性测试结果/强制解绑/风控限额设置 |
| 风控中心 | 用户级参数：最大单笔名义/日亏限额/最大仓位/允许交易对/实盘开关 |
| 审计 | 全用户操作日志筛选导出（登录/交易/改参/密钥操作） |
| 公告 | 平台公告/风险提示推送（交易室横幅） |
| 数据面板 | 注册数/活跃数/订阅收入/实盘交易量 |

### 3.3 实盘交易链路（核心新增）

```
用户已绑定密钥 → 实盘模式开启 → 策略信号(现有引擎) → 实盘引擎
  → Bybit v5 /v5/order/create (签名HMAC) / PM CLOB order (EIP-712或Relayer)
  → 订单状态轮询/对账(每60s拉持仓余额) → 本地台账 → 审计留痕 → 用户看板
风控前置: 用户限额/单笔上限/日亏熔断/交易对白名单/密钥连通性自检
```

## 四、数据模型（核心表）

```
users(id, username, email, pass_hash, role, status, plan, plan_expires,
      failed_attempts, locked_until, created_at, last_login_at)
user_keys(id, user_id, venue, label, key_enc, secret_enc, status,
          test_result, last_test_at, created_at)      -- 加密存储
user_limits(id, user_id, max_notional, daily_loss_cap, max_positions,
            allowed_pairs, live_enabled)
plans(id, code, name, price, features) / subscriptions(id, user_id, plan_id,
            start_at, expires_at) / invoices(...)
audit_logs(id, ts, user_id, action, detail, ip, ua)
```

纸面状态沿用现有文件结构，按 `data/users/<uid>/logs/...` 分目录（引擎 BASE 路径参数化）。

## 五、实施里程碑（~6 轮，按风险排序）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M1 认证与用户体系** | users 表/argon2/图形验证码/登录限流/JWT会话/角色中间件/注册登录页；现有单用户迁移为 admin | 注册→登录→会话隔离；爆破5次锁定 |
| **M2 多租户数据隔离** | 每用户独立状态目录+参数+引擎上下文（行情共享）；API 全路由加 uid 作用域 | 两用户互不可见/不可操作对方账户 |
| **M3 总管理后台** | 用户管理/套餐/审计/公告/数据面板（独立 /admin 入口+页面） | admin 全操作闭环+审计留痕 |
| **M4 密钥托管** | 绑定/加密存储(AES-GCM)/连通性测试/解绑/限额配置 + 用户端UI | 密钥永不落日志/不返回明文；测试通断准确 |
| **M5 实盘引擎** | M5a Bybit 实盘（签名下单/撤单/持仓余额对账）；M5b PM 实盘（CLOB下单/钱包签名） | 纸面与实盘结果偏差<阈值；订单全幂等 |
| **M6 安全与商业化加固** | 全API限流/2FA(TOTP)/服务条款与风险披露/账单支付接口(预留)/渗透自测 | 安全自测清单全过 |

## 六、安全与合规清单（商业化硬门槛）

- [ ] 密码 argon2id；密钥 AES-GCM+主密钥环境变量；日志脱敏（密钥/密码永不入日志）
- [ ] 登录/注册/密钥端点限流；账号锁定；验证码防机器人
- [ ] 会话 HttpOnly/Secure/过期；权限中间件（admin/user 路由隔离）
- [ ] 审计全留痕（IP）；敏感操作二次确认
- [ ] 实盘风险披露/服务条款/PM 地理限制提示（用户确认后才可开实盘）
- [ ] 密钥托管与 BYOK 双模式说明
- [ ] 数据备份策略（用户状态每日备份）

## 七、交付物与验收

- 交付：认证中心/验证码/多租户引擎改造/总后台/密钥保险库/实盘引擎(Bybit+PM)/审计/计费骨架 + 单测 ~60 项 + 部署文档
- 验收链：M1-M4 每轮全量回归（现有单用户功能不回归）→ M5 实盘先小金额($25)演练 → M6 安全自测 → 灰度放量
- 预估 ~2500 行，6 轮交付；**建议商业顺序：先上线「多用户模拟交易」（M1-M3），再上线「密钥托管实盘」（M4-M6）**，两阶段可独立收费运营

**一句话**：把现有单用户系统改造成「中心行情数据+多用户独立账户+管理后台+密钥保险库+实盘引擎」的 SaaS 架构，模拟交易先行获客，实盘托管后行变现。
