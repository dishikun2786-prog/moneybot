# 波段量能 + 小金额单边对冲 + 循环恢复 — 交易执行策略开发计划

> 2026-09-22 · 基于现有栈（微结构数据/裸腿基建/双向套利/波段评分已全部上线）
> 用户思路：波段量能择时 + 小金额对冲单边套利 + 亏损反向倍投 + 循环
> 本计划将该思路**工程化落地**，并对「倍投」组件做**专业风险改造**（原始倍投数学上不可行，见 §2，替代方案见 §3）。

## 一、策略思路拆解（三段式循环）

```
┌─────────────────────────────────────────────────────┐
│  ① 波段量能择时 (swing score 已有)                    │
│     CVD斜率 + 主动量失衡 + 挂单墙 + OI变化 → 0-100分   │
│     评分≥阈值 → 触发本循环回合                          │
├─────────────────────────────────────────────────────┤
│  ② 小金额单边对冲 (裸腿基建已有)                       │
│     开 1 倍基础单 (名义 $10 档)                        │
│     方向 = 波段评分方向 (顺势) 或 funding 方向 (套利)    │
│     强制止盈止损 (已有双向TP/SL校验)                    │
├─────────────────────────────────────────────────────┤
│  ③ 循环结算与恢复判定                                  │
│     盈利 → 回落到基础单, 开始下一回合                    │
│     亏损 → 进入恢复阶梯 (受限倍投, 见 §3)               │
└─────────────────────────────────────────────────────┘
```

白话：**每一回合都等盘口给出明确的方向信号才出手，单子很小，亏了不硬扛、按规则做恢复，赚了就复位重新循环。**

## 二、专业风险评估：原始「亏损倍投」为什么必须改造

### 2.1 倍投的数学本质（白话+公式）

倍投（Martingale）的承诺是「只要一直翻倍下注，赢一次就能回本」。数学真相：

- **负期望交易 × 倍投 = 更大的负期望**：若单笔期望 E<0，n 笔独立下注期望 = n·E，倍投只是把亏损**集中成小概率大亏损**（尾部风险），期望不变差但**最大回撤按 2^n 爆炸**。
- **连续亏损是必然事件**：单笔胜率 p=50% 时，连续 8 次亏损概率 = (1-p)^8 ≈ 0.39%——看似很小，但**每 256 个回合就发生一次**，而 8 次翻倍后仓位 = 256 倍基础单！
- **资金有限性判死刑**：基础 $10、翻倍 10 次 = $10,240。我们的复利上限是名义 3 倍（$30 档）——原始倍投**第 2 级就撞上限**。

### 2.2 结论

| 组件 | 判定 |
|---|---|
| 波段量能择时 | ✅ 正期望组件（已有数据+评分，待积累数据验证） |
| 小金额单边对冲 | ✅ 风控友好（单腿敞口小、强制TP/SL） |
| **原始倍投** | ❌ **必须改造**（尾部风险与复利上限冲突，数学上不可持续） |
| 循环机制 | ✅ 合理（回合制+复位，利于纪律与统计） |

## 三、专业修正：受限恢复阶梯（Bounded Recovery Ladder）

保留用户思路的「亏损后加大投入回本」**直觉**，用三条护栏把它变成可执行的规则：

### 3.1 规则（替代原始倍投）

| 护栏 | 规则 |
|---|---|
| **倍率减半** | 每级 ×1.5（非 ×2）：基础 $10 → 恢复1级 $15 → 恢复2级 $22.5 |
| **级数封顶** | 最多 3 级，之后**强制复位到基础单**（放弃本轮回本，止损循环） |
| **敞口上限** | 恢复单名义 ≤ 复利上限（3×基准）——天然满足 |
| **信号门槛** | 恢复级开仓**必须波段评分再次确认**（≥阈值才加码，亏损≠加码理由） |
| **风险预算隔离** | 恢复阶梯只动用「当日利润池」：当日累计亏损 ≥ 日亏限额（$2，复用现有熔断）→ 冻结循环 24h |
| **方向独立** | 恢复单方向由当前信号重新判定（可与上一单同向/反向），**不做情绪化反向** |

### 3.2 期望与回撤模拟（需回测验证的指标）

- 单回合期望 E = p·(TP收益) − (1−p)·(SL损失) − 手续费
- 恢复阶梯总敞口上界 = 10 + 15 + 22.5 = $47.5 ≤ 3×$10 复利上限的**预算独立核算**
- 最大连续亏损假设检验：连续 5 回合亏损概率与阶梯回撤曲线（回测输出项）
- **验收硬指标**（达标才开实盘纸面）：
  1. 单回合胜率 ≥ 45%（波段择时应有正向加持）
  2. 策略年化 ≥ 纯 funding 持有基线的 80%（不劣化）
  3. 最大回撤 ≤ 5%（当前复利体系容忍线）
  4. 尾部检验：模拟 1000 次蒙特卡洛，5% 分位回撤 ≤ 8%

## 四、开发计划（M1-M5）

### M1 — 策略核心模块（~200 行）
- `swing_cycle.py`：回合状态机（IDLE→ARMED→OPEN→SETTLED）
  - 回合状态持久化 `logs/swing_cycle.json`（与引擎共享 fcntl 锁）
  - 开仓条件：swing_score ≥ 阈值（复用 carry_engine.swing_score）+ 资金/敞口检查
  - 恢复阶梯状态（当前级数、累计盈亏、冻结标记）
- 复用 paper_ops 的 open_hedge/close_spot_to_naked/close_naked（单边对冲=裸腿带TP/SL）
- 单元测试：状态机全路径、阶梯升级/复位、冻结逻辑

### M2 — 结算与循环引擎（~150 行）
- 每 60s tick：检查裸腿 TP/SL 触发（已有）→ 回合结算
- 结算判定：盈利 → 复位阶梯；亏损 → 阶梯+1（≤3）；阶梯耗尽 → 复位+记录
- 循环审计 `logs/swing_rounds.jsonl`：每回合（信号分/方向/名义/结果/阶梯级数/耗时）
- 单元测试：结算判定矩阵

### M3 — 回测验证（**上线前硬门槛**，~250 行）
- `tools/backtest_cycle.py`：用 trades_1s + micro_1m + price_1s 历史（现约 27h，继续积累）回放：
  - 波段评分信号 → 开单 → TP/SL 结算 → 阶梯恢复
  - 输出 §3.2 四项验收指标 + 蒙特卡洛尾部检验
- **数据不足 2 周时的正确姿势**：先跑参数敏感性扫描（阈值/倍率/级数网格），产出「哪个参数组合最稳」的热力图；数据达标后跑正式验收
- 达标 → 进入 M4；未达标 → 只调参重测，不得上线（与现有 AI 改参三闸同纪律）

### M4 — 纸面实盘（隔离运行）
- 以**独立账户分区**运行（day_pnl 独立核算），与现有套利/PM 仓位互不干扰
- 默认关闭（`swing_cycle_enabled=0` 参数），手动/AI 审批开启
- 交易室新增「循环策略」卡片：当前回合/阶梯级数/信号分/累计结果 + 复盘表
- 观察期 ≥ 72h（复用纸面观察纪律），每日自动复盘报告（cron）

### M5 — 评估与迭代
- 72h 实盘 vs 回测对比（实现偏差检测）
- 参数微调走现有审批流；风险指标恶化 → 自动熔断（日亏限额）

## 五、安全护栏汇总

| 护栏 | 机制 |
|---|---|
| 敞口硬顶 | 恢复阶梯总名义 ≤ 3×基准（复利上限复用） |
| 日亏熔断 | 当日亏 ≥ $2 → 循环冻结 24h（复用 MAX_DAILY_LOSS 逻辑） |
| 级数封顶 | 3 级后强制复位，拒绝无限翻倍 |
| 信号纪律 | 每级开仓必须评分重新确认（亏损不是加码理由） |
| 模式隔离 | 循环策略独立开关，与托管/手动模式正交 |
| 审计 | 每回合全留痕 + 每日复盘 |

## 六、交付物与验收

- 交付：swing_cycle.py / 结算引擎 / 回测工具+报告 / 交易室循环卡片 / 单测 ~30 项 / 回合审计
- 验收链：**单测 → 参数扫描 → 2 周数据回测达标（四项硬指标）→ 72h 纸面观察 → 实盘开关**
- 预估 ~700 行，3 轮交付；核心风险点（倍投尾部）在 M3 回测环节强制暴露并设卡

**一句话总结**：保留「波段择时 + 小单 + 循环恢复」的完整思路，把「无限倍投」改造成「×1.5、3 级封顶、信号确认、利润池隔离」的受限恢复阶梯——赚同样的直觉收益，但把破产风险从"迟早发生"变成"被规则物理封死"。
