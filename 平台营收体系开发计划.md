# 平台运营营收体系开发计划（费率配置 + 营收大盘）

日期：2026-09-25 ｜ 范围：管理后台(admin) + 纸面交易引擎(paper_ops) + 留痕体系 ｜ 目标：商业级运营配置与营收可视

---

## 一、现状与差距

| 项目 | 现状 | 差距 |
|---|---|---|
| 费率 | 代码硬编码常量（现货 10bp / 永续 5.5bp / 原生 5bp），VIP 等级乘数 _fee_mult() | **无按标的自定义、无 admin 界面、改动要发版** |
| 点差 | 无概念，纸面盘按实时价直接成交 | **平台无法从点差赚营收** |
| 营收 | carry_trades.jsonl 有 fees 字段，但无「平台营收=用户费-官方成本」的拆分与汇总 | **运营看不到平台赚多少钱** |
| 审计 | 有 /api/audit | 配置变更需并入审计 |

## 二、商业定价模型（核心设计）

**平台营收 = 费率加价 + 点差，两条腿：**

1. **费率加价（倍率模型）**：每标的（×现货/永续两通道）设「平台加价倍率」`mult`（默认 1.00）
   - 用户实付费 = 官方费率 × mult
   - 平台营收 = 用户实付费 − 官方成本（官方成本 = 官方费率 × 名义，即 Bybit 真实收走的）
   - 例：BTC 永续官方 5.5bp，mult=2.0 → 用户付 11bp，平台净赚 5.5bp
   - 上限保护：mult 范围 0.5–5.0（低于 1 即补贴，可做运营活动）
2. **点差（spread）**：每标的设点差 `spread_bp`（默认 0）
   - 纸面成交价 = 市价 × (1 ± spread_bp/2/10000)：买入加半档、卖出减半档
   - 平台营收 = 名义 × spread_bp / 10000（点差全额归平台）
   - 上限保护：≤ 50bp，防止离谱价远离市价

**定价原则**：官方费率取自 Bybit 真实档位（BTC/ETH 永续 5.5bp、其余 5.5bp、现货 10bp——配置页预填「官方值」列只读参考），管理员只需改「加价倍率」和「点差」，不用背官方数字。

## 三、数据模型

**新文件 `data/fee_config.json`**（git 忽略，随部署保留）：
```json
{
  "updated_by": "admin", "updated_at": "2026-09-25T...",
  "symbols": {
    "BTCUSDT": {"perp_mult": 1.0, "spot_mult": 1.0, "spread_bp": 0},
    "XAUTUSDT": {"perp_mult": 1.0, "spot_mult": 1.0, "spread_bp": 5}
  },
  "default": {"perp_mult": 1.0, "spot_mult": 1.0, "spread_bp": 0}
}
```

**留痕增强**（carry_trades.jsonl 新增字段，老记录不受影响）：
- `user_fee`：用户实付费；`official_fee`：官方成本；`platform_rev`：平台营收（= user_fee − official_fee + 点差收益）；`spread_rev`：点差收益；`mult`：当次生效倍率
- 老记录回算规则：`official_fee = user_fee / 历史倍率(默认1.0)`，营收=0（历史不加价）

## 四、执行链路改造（paper_ops 全接入点）

| 接入点 | 现状 | 改造 |
|---|---|---|
| `_fee_mult()` | 按租户 VIP 等级乘数 | **改为：租户 VIP 乘数 × 标的平台加价倍率**（两段相乘） |
| 成交价 `_prices()` | 实时价 | 下单/平仓瞬间按标的 spread 偏移 |
| `_log_trade` 调用点（开/平/现货/对冲/裸腿/原生 6 类） | 只记 fees | 统一记 user_fee/official_fee/platform_rev/spread_rev/mult |
| 资金对账 `day_pnl` | 扣 fees | 继续扣用户实付费（不变） |

**风险控制**：spread 偏移只作用于纸面成交价，**不污染行情展示**（盘口/K线仍显示真实市价）；托管引擎（carry_engine）的套利触发价也用偏移后价格（与纸面盘一致）。

## 五、后端 API（dash/app/admin.py 或新 ops.py）

1. `GET /api/admin/fee-config` → 全标的白名单配置 + 官方费率参考表（只读）
2. `PUT /api/admin/fee-config` → 批量保存（白名单参数校验 + 范围钳制 + 审计留痕 + 广播刷新）
3. `GET /api/admin/revenue?from&to&by=symbol|user|action|day` → 营收汇总（区间/维度）
4. `GET /api/admin/revenue/detail?offset&limit&from&to` → 营收明细（倒序分页：时间/用户/标的/动作/用户费/官方成本/平台营收/点差收益）
5. `GET /api/admin/revenue/export` → CSV 导出（运营对账用）

**权限**：全部 require admin（uid=1）；配置变更写审计流。

## 六、admin 前端（两个新面板）

**① 「费率配置」面板**：
- 标的 × 通道矩阵表格：标的 | 官方现货费率(只读) | 现货加价倍率 | 官方永续费率(只读) | 永续加价倍率 | 点差bp | 预览用户实付费
- 批量默认值行 + 一键全默认 + 保存（防误触二次确认）+ 变更历史（最近 10 次）
- 白话提示：「用户实付费 = 官方费率 × 加价倍率；点差全额归平台」

**② 「营收大盘」面板**：
- 顶部 4 卡：今日营收 / 累计营收 / 今日笔数 / 客均营收
- 图：近 30 日营收趋势（按日）
- 表：按标的营收排行（含点差/费率拆分）
- 表：按用户营收排行 TOP20
- 明细表：倒序 + 筛选（用户/标的/动作）+ CSV 导出

## 七、验收标准（真机）

1. 设 BTC 永续 mult=2.0 → 用户开 100$ 名义 → 记录 user_fee=11bp、platform_rev=5.5bp ✓
2. 设 XAUT 现货 spread=10bp → 买入成交价=市价×1.0005 ✓、营收含点差 ✓
3. 营收大盘 4 卡 + 趋势图 + 双排行 + 明细 + 导出全通 ✓
4. 配置保存后旧仓平仓用新配置（实时生效）✓、审计流有记录 ✓
5. VIP 等级乘数与标的加价正确叠加 ✓（专业版用户同样被加价）
6. 行情展示不受 spread 污染 ✓（盘口仍真实市价）

## 八、分期与工期

| 阶段 | 内容 | 工期 |
|---|---|---|
| A 数据与引擎 | fee_config.json + 6 接入点改造 + 留痕字段 + 回算 | 1 天 |
| B 后端 API | 5 端点 + 校验 + 审计 + 营收聚合 | 1 天 |
| C admin 前端 | 费率配置面板 + 营收大盘（图表+明细+导出） | 1 天 |
| D 验收部署 | 真机 6 项验收 + 冒烟回归 + 双端提交 | 0.5 天 |

**商业注意点**（需您决策，见下）：现有用户对「0 加价」的既有承诺——默认 mult=1.0/spread=0，新配置只影响新成交；如要对存量用户加价，建议公告先行（公告面板已有）。

---

**确认后开工。默认方案：全部标的白名单 8 个 + 两通道 + 点差，mult 默认 1.0，spread 默认 0。**
