# USDT 充值提现体系 — 方案设计与开发计划 (M7)

> 目标：移动端 USDT 充值（Bybit 地址收款 + 自动回调闭环）+ 提现（审核打款）+ 平台余额体系 + 余额购买套餐。
> 状态：可行性已实测（Bybit v5 asset 三端点存活、签名客户端复用 bybit_live._req），等待确认后实施。

---

## 一、核心方案（可行性已验证）

### 1. 收款方式
- **平台 Bybit 主账户 USDT 充值地址**（用户现有 UTA 统一账户），主链 **TRC20**（到账 1-2 分钟、费率低），可扩展 ERC20
- 地址获取：`GET /v5/asset/deposit/query-address?coin=USDT&chainType=TRC20`（只读 key 即可，实测端点存活）

### 2. 用户识别：金额匹配法（关键设计）
TRON USDT 转账不支持 memo，采用「**唯一金额尾数**」识别：
- 用户发起充值 → 系统生成订单 + 唯一金额（如 **100.0047** USDT，尾数 4 位在活动订单中唯一）
- 用户按精确金额转账 → 对账服务匹配订单 → 自动入账
- 充错金额 → 进「待认领池」，用户提交 txid 或管理员人工分配
- 备选（二期）：Bybit 子账户每用户独立地址——第一期不做，太重

### 3. 充值回调闭环（状态机）
```
pending(待付款) → matched(链上已匹配) → confirmed(已入余额 ✅)
     ↓超时24h                 ↓金额不符
  expired(过期)           unclaimed(待认领池 → 管理员分配)
```
- **对账 cron 每 60s**：`GET /v5/asset/deposit/query-record?coin=USDT`（只读 key）→ 按币种+金额匹配 pending 订单 → confirmed → 余额 + 流水 + 站内通知
- 兜底：用户可提交 txid + 金额 自助申报 → 管理员核对链上记录入账

### 4. 提现闭环
```
申请(金额+TRC20地址) → 管理员审核 → Bybit API 打款(withdraw/create) → 状态追踪(query-record) → 完成
                         ↓拒绝                    ↓打款失败(余额不足/风控)
                       退回余额                  状态回退+管理员处理
```
- Bybit 提现需 **withdraw 权限的 API key**（与交易 key 分离，权限最小化）+ 管理员二次确认
- 平台政策：TRC20 提现费 1 USDT（可配）、单笔/当日限额（user_limits 扩展）

### 5. 余额体系（users.db 新表）
| 表 | 内容 |
|---|---|
| `balance` | uid → usdt 余额 |
| `balance_tx` | 全部流水（充值/提现/套餐消费/系统调整，审计留痕） |
| `deposit_orders` | 充值单（唯一金额/链/地址/状态/txid） |
| `withdraw_orders` | 提现单（金额/地址/手续费/状态/txid） |

### 6. 套餐自助购买
- 定价：**专业版 pro = 30 USDT/月**（Bybit/PM 实盘绑定权限）、**实盘版 live = 99 USDT/月**（实盘交易+全风控），免费版 0（现有）
- 余额支付 → plan 更新 + 到期时间 → 到期自动降级 cron（每日检查）
- 定价待你确认（上面是建议值）

---

## 二、开发阶段（M7，预计 5-7 个工作日当量）

| 阶段 | 内容 | 验收 |
|---|---|---|
| **M7-A 余额后端** | balance/balance_tx/deposit_orders/withdraw_orders 四表 + 流水/查询接口 + 管理接口 | 测试：充值入账/流水完整性/并发安全 |
| **M7-B 充值闭环** | 订单生成（唯一金额）+ 地址 API 集成 + 对账 cron（60s）+ 认领池 + txid 自助申报 | e2e：模拟链上记录 → 自动入账 → 余额到账；充错金额进认领池 |
| **M7-C 提现闭环** | 申请（限额/地址校验）→ 审核 → Bybit 打款 API + 状态追踪 → 完成/拒绝 | e2e：申请→审核通过→打款状态流转；限额拦截 |
| **M7-D 套餐购买** | 定价配置 + 余额支付 + 到期自动降级 cron | 测试：支付→plan 生效；到期→降级 |
| **M7-E 移动端 UI** | 资产屏余额卡 + 充值页（链选择/地址复制/二维码/状态轮询）+ 提现页 + 套餐页 + 我的入口 | 移动端冒烟 + 生产 e2e 全点击路径 |
| **M7-F 管理后台** | admin.html 加：充值订单管理/认领池分配/手动入账/提现审核/余额调整 | 管理员 e2e 全链路 |

---

## 三、移动端 UI 交互设计

### 1. 资产屏（screen-assets 顶部）
```
┌─────────────────────────────────┐
│  USDT 余额            ¥≈7.2/USD │
│  💰 128.50 USDT       [充值][提现]│
│  今日 +100 / -0 · 累计充值 350    │
└─────────────────────────────────┘
┌─ 账户套餐 ──────────────────────┐
│  当前：免费版  [⬆ 升级套餐]      │
└─────────────────────────────────┘
```

### 2. 充值页（新屏幕 recharge）
```
USDT 充值
┌─ 选择链 ──────────┐
│ ✓ TRC20(推荐)  ERC20 │
└──────────────────┘
┌─ 充值地址 ───────────────────┐
│ TQrY8...9xK   [📋 复制]      │
│ ████████ (二维码 SVG)         │
│ 仅支持 USDT-TRC20，充错链丢失 │
└──────────────────────────────┘
┌─ 重要：精确金额 ──────────────┐
│ 请转账 **100.0047** USDT      │
│ (尾数用于自动识别到账，勿改)   │
└──────────────────────────────┘
⏳ 等待付款… (60秒自动刷新)
   └ 已到账 → ✅ 充值成功 +100 USDT
充值记录：[时间 金额 状态] 列表
```

### 3. 提现页（新屏幕 withdraw）
```
USDT 提现
┌─ 金额 ──────────────┐
│ [50.00] USDT  [全部] │
│ 可用余额 128.50      │
└─────────────────────┘
┌─ TRC20 提现地址 ─────┐
│ [粘贴地址…]  [校验]   │
└─────────────────────┘
手续费 1 USDT · 实际到账 49.00 USDT
[⚠ 提交申请] → 管理员审核(工作时间) → 打款
提现记录：[状态: 待审核/打款中/已完成/已拒绝]
```

### 4. 套餐页（新屏幕 plans）
```
升级套餐
┌ 免费版 ── 0 USDT ─────────┐
│ 纸面模拟交易 · 行情 · 社区  │
├ 专业版 ── 30 USDT/月 ─────┤
│ + Bybit/PM 密钥绑定(实盘准备) │  [余额购买]
├ 实盘版 ── 99 USDT/月 ─────┤
│ + 实盘交易 · 全风控 · 优先支持 │ [余额购买]
└──────────────────────────┘
当前套餐：免费版 · 到期 无
余额不足 → 提示先充值
```

### 5. 我的屏
- 加「USDT 余额」入口（余额+流水页）

### 桌面管理后台（admin.html 新增「资金管理」区）
- 充值订单：pending/matched/confirmed/expired 列表 + 待认领池（金额+txid → 分配给用户）+ 手动入账
- 提现审核：申请列表（用户/金额/地址）→ ✅通过打款 / ❌拒绝（理由）
- 余额调整：对用户 +/- USDT（必填理由，审计留痕）

---

## 四、需要你准备（实施前）

1. **Bybit 只读 API key**：用于查充值地址 + 充值记录对账（权限最小：只读钱包/资产）
2. **Bybit 提现 API key**（可选）：开启 withdraw 权限，否则提现改为「管理员在 Bybit 网页手动打款 + 系统记录状态」
3. **确认套餐定价**：专业版/实盘版月费（建议 30/99 USDT）
4. **确认提现政策**：手续费（建议 1 USDT 覆盖 Bybit 成本）、单笔/当日限额

## 五、风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| Bybit API 限频(120/min) | 低 | 对账 cron 60s 一次，仅拉最近 10 条 |
| 金额尾数撞车 | 低 | 活动订单中唯一性校验，冲突顺延；超时过期释放 |
| 用户充错金额 | 中 | 认领池 + txid 申报 + 管理员人工核对 |
| 提现打款失败 | 中 | 状态回退 + 管理员处理 + 打款前余额预检 |
| API 密钥泄露 | 高 | 提现 key 独立存储（AES-GCM 复用 M4 保险库）+ 权限最小 + 审计 |
| 合规 | 中 | 充值页明示「充错链/金额责任自担」；条款更新 |

## 六、交付顺序建议

**M7-A → M7-B → M7-E(充值部分) → M7-C → M7-D → M7-E(提现/套餐) → M7-F**
先打通「充」（钱进来+自动入账+移动端可见），再打通「提」（审核打款），最后套餐购买闭环。
