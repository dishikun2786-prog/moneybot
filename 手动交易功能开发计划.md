# 交易室手动交易功能 + 专业交互 UI + 移动端自适应 — 开发计划

> 2026-09-22 · 全部为纸面交易（模拟盘），不涉及真实资金
> 背景：交易室目前只能"看"（K线/持仓/成交带），本计划让它能"操作"

## 一、需求拆解（5 项）

| # | 需求 | 说明 |
|---|---|---|
| R1 | 现货合约**单向平仓** | 手动平掉持仓的**合约腿**（现货腿按引擎语义转"孤儿腿"自动复用）|
| R2 | 现货合约**全平** | 双腿同时市价平仓（完全退出该标的）|
| R3 | 预测市场**手动平仓** | 纸面 PM 桶按当前盘口买/卖价平掉 |
| R4 | **一键对冲开仓** | 点一下：读实时价（WSS桥 ≤0.2s 新鲜度）→ 双腿纸面开仓 |
| R5 | 专业交互 UI + 移动端自适应 | 持仓卡片操作按钮/确认弹窗/执行反馈/触屏优化/响应式布局 |

## 二、设计决策

### 2.1 单向平仓语义（关键）
- ✅ **平合约腿**：合约腿按实时价平掉 → 现货腿转"孤儿腿"（引擎现有复用逻辑接管，与自动单边平仓同语义）
- ✅ **全平双腿**：现货+合约同时平
- ❌ **只平现货腿**（留下裸空合约）= v2 再议（需引擎新增"裸腿持仓"风控模型，v1 不做——避免纸面账户出现无保护的裸方向敞口）

### 2.2 执行价格
| 腿 | 价格来源 |
|---|---|
| 现货/永续 | **bybit_prices.json 实时快照**（WSS桥，≤0.2s；回退 carry_1m 最新行）|
| PM 桶 | 点击时实时拉 CLOB 盘口（32ms，long 用卖一/short 用买一；回退 book_1s）|

### 2.3 状态一致性（引擎并发）
- 新建共享模块 `paper_ops.py`：引擎与 API 共用同一套状态读写 + **fcntl 文件锁**
- 手动操作与引擎 60s tick 通过锁串行化 → 不会互相覆盖
- 手动操作走引擎同一套会计：PnL 计入 `day_pnl`、交易写 `carry_trades.jsonl` / `paper_trades.jsonl`、动作带 `MANUAL_` 前缀

### 2.4 审计与安全
- 全部手动操作写独立审计 `logs/manual_actions.jsonl`（时间/动作/标的/价格/名义/结果）
- 成交带新增动作中文映射：手动平合约腿/手动全平/手动平仓PM/手动一键对冲
- 端点要求登录态；**纸面交易无资金风险**，不设审批流（与 AI 改参不同——不改变策略行为）

## 三、实施里程碑

### M1 — 后端手动交易 API（核心，~150 行）
- 新建 `paper_ops.py`：`close_perp_leg(sym)` / `close_both(sym)` / `close_pm(market_key)` / `open_hedge(sym, notional)` + 文件锁 + 实时价获取
- main.py 新增 `POST /api/manual/trade`：`{action, symbol}` → 校验（持仓存在/名义合法）→ 执行 → 返回结果+审计
- carry_engine / paper_engine 改为通过 paper_ops 的锁读写状态（防竞态）
- readers：positions API 附带可操作标记（是否持仓/孤儿/PM仓位）+ 新动作中文翻译

### M2 — 交易室交易 UI（~200 行）
- **持仓面板重构**：每个持仓卡片化 → 现货价/合约价/当前浮盈 + 操作按钮（「平合约腿」「全平」）
- **PM 持仓**：平仓按钮（显示当前盘口价）
- **快速交易票**（右栏顶部）：标的切换 + 实时价大字（WSS已有）+ 名义输入 + **「一键对冲开仓」**按钮
- **确认弹窗**：显示将要执行的动作/腿/价格/预估费用，二次确认
- **执行反馈**：Toast（成功/失败/原因）+ 成交带即时刷新（新动作高亮）
- 空仓/无权限状态提示

### M3 — 移动端自适应（~120 行）
- 重构媒体查询（375px / 768px 断点）：
  - 顶栏 flex-wrap，导航变横向滚动条，livebox 换行
  - 持仓卡片全宽堆叠，按钮触控目标 ≥44px
  - 成交带表横向滚动 + 列精简（移动端隐藏备注列）
  - 聊天抽屉/确认弹窗改全宽底部弹层（bottom sheet）
  - K线图高度按屏幕比例，横屏提示
- ECharts orientationchange 重绘（现有 resize 监听升级）

### M4 — 验证与演练（~1 轮）
- 端到端实测：一键对冲 → 持仓出现双腿 → 手动平合约腿 → 现货转孤儿 → 引擎复用 ✓
- PM 平仓会计正确性（已实现/未实现 PnL 归位）
- 并发演练：手动操作与引擎 tick 交错 100 次无状态损坏
- 移动端：Chrome DevTools 375px 模拟 + 真机自测（用户）

## 四、交付物清单
| 文件 | 内容 |
|---|---|
| `paper_ops.py` | 共享交易操作模块（锁+执行+审计）|
| `dash/app/main.py` | /api/manual/trade 路由 |
| `dash/app/readers.py` | 持仓可操作标记 + 手动动作翻译 |
| `dash/static/trade.html` | 持仓卡片/快速交易票/确认弹窗/Toast |
| `dash/static/trade.html` CSS | 移动端断点重构 |
| `carry_engine.py` / `paper_engine.py` | 接入共享锁 |
| `tests/test_paper_ops.py` | 手动操作单元测试 |

## 五、风险与对策
| 风险 | 对策 |
|---|---|
| 手动操作与引擎 tick 竞态 | fcntl 文件锁串行化（M1）|
| 手动平仓价格滑点 | 纸面按实时快照成交；显示与成交价差异提示 |
| 误触操作 | 全部二次确认弹窗 + 执行前显示腿明细 |
| 移动端 ECharts 性能 | 移动端降低 K线渲染量（减少 dataZoom 粒度）|
| 用户会话在移动设备未登录 | 沿用现有登录态（Mac 设备 401 问题一并提示登录）|

## 六、预估与验收标准
- 总代码量 ~500 行（后端 150 / 前端 320 / 测试 30），1-2 轮完成
- 验收：① 一键对冲后持仓与浮盈正确 ② 手动平合约腿→孤儿复用闭环 ③ PM 平仓 PnL 正确 ④ 375px 宽度下全部可操作 ⑤ 全部操作留审计
