# UI 审查报告与修复计划 (R 轮)

## 审查结论 (2026-09-23 实测)

### 问题 1：现货标的点击无法进入，提示错误
- **根因**：`mkTap()` 中现货标的若没有同名永续合约 → 直接 `toast('暂只支持现货行情')` 拦截返回。全站只有「交易屏」一个标的落地页，而交易屏只做合约。纯现货（USDTTRY、BTCUSDC）无路可走。
- **实测**：USDTTRY/BTCUSDC 点击 → 提示错误 ✓ 复现。

### 问题 2：USDTTRY/BTCUSDC 现货数据为 0
- **根因**：**spot WS 通道 537 个 ticker 全量订阅超过 Bybit 限流** → 服务器日志实测"spot连接中断, 5s后重连"循环，排在订阅列表尾部的标的永远收不到数据。
- **实测证据**：单独订阅 `tickers.BTCUSDC/USDTTRY/ETHUSDC` 15 秒内全部收到（85920/48.78/2736.44）✓；但桥内全量订阅时快照中两标的为 None ✗。
- **附带发现**：快照 1130 标的 = 885 线性 + 仅部分现货（后段现货全丢）。

### 问题 3：实盘没有蜡烛图实时专业数据
- **根因**：`loadKline()` 画的是 **polyline 折线**（仅收盘价连线），无 OHLC 蜡烛柱、无成交量；K 线数据走 REST 回源 60 秒缓存，非实时。
- 桥内 `kline.1` 实时通道只订阅 BTC/ETH 两个标的。

### 问题 4：PM 标的不全
- **根因**：`pm_clob_ws.py` 只拉 gamma **Top 60** 事件（1946 tokens / 仅 56 个有实时价）；移动端 PM 屏只有一个下拉选择器，无市场列表/分类/搜索。

---

## 修复计划 (R1-R6)

| 轮 | 内容 | 效果 |
|---|---|---|
| **R1** | spot 通道**分批限速订阅**（每批 20 个、间隔 2s）+ 订阅失败自动重试 + 订阅完成前不判死 | BTCUSDC/USDTTRY 实时价恢复 |
| **R2** | `/api/instruments` 返回 REST 缓存价；移动端现货 Tab 缺失实时价时显示缓存价兜底 | 现货行情永不空白 |
| **R3** | 现货标的点击 → 新增「标的详情屏」（行情+蜡烛+数据，现货合约通用）；现货不再被拦截 | 所有标的可点击进入 |
| **R4** | K 线组件重写为**专业蜡烛图**：OHLC 蜡烛柱 + 成交量副图 + 时间轴 + 实时末根（桥新增按需 kline 订阅，LRU 同盘口机制） | 实盘蜡烛图实时专业数据 |
| **R5** | PM 扩容：gamma Top 60 → **Top 150 事件** + 分类字段；移动端 PM 屏重做：分类 Tab（加密/体育/政治/文化…）+ 市场卡片（标题/YES价进度条/成交量/涨跌）+ 搜索，点击进下单 | PM 标的全覆盖 + 原生交互 |
| **R6** | 全量测试 + 部署 + 实测验证 | 上线 |

### 关键设计
- R1 分批订阅后仍超限的兜底：spot 降为「成交额 Top 300」订阅 + 其余 REST 兜底（R2 已覆盖显示层）。
- R4 按需 K 线：`/api/kline/watch {symbol, interval}` 写 watch 文件 → 桥 2 秒内订阅 `kline.<iv>.<sym>`（LRU 8）→ `/api/klines` 优先读实时缓存，末根 100ms 级更新。
- R5 PM 分类：gamma events 自带 category（politics/sports/crypto/culture/science/business…），按分类 Tab 过滤；订阅量 Top150 事件约 3000+ tokens，按「每个市场只订 2 个主力 token + 盘口活跃 token」控制总量，超出分批。
