# M5 规模与合规审查报告

> 日期：2026-09-25（UTC+8）｜版本：M5 收尾版｜审查对象：Moneybot 商业化交易平台全栈

## 一、本轮（M5）交付

| 项 | 结果 | 证据 |
|---|---|---|
| 数据备份加固 | ✅ | 机密文件（.platform_keys.json/.dash_secret/.dash_passwd_hash/.ai_secrets.json）纳入每日 03:00 备份；实测 9 项归档成功 |
| swing_score 中期复测 | ⏸ 继续关 | 47h 数据回测：BTC 命中率 49.8%、ETH 49.2%（≈随机无预测力）；2 周窗口未到（约 9-30 后复测） |
| 现货-永续基差监控 | ✅ | bridge basis 帧（5 双通道标的）+ 行情屏基差卡片；真机 5 标的实时渲染（BTC -0.038% 等）零 JS 错误 |
| 租户隔离修复 | ✅ | 13/13（M3 装饰器 bug 污染 admin state 已清） |

## 二、负载与容量评估（实查）

- **服务器**：1 核 2G（都柏林 AWS Lightsail），可用内存实测 1.2–1.3 GB
- **常驻服务 5 个**：pm-dash(8080)、pm-bridge（WSS 多连接 + 8 标的合约 + 6 标的现货双通道）、pm-carry、pm-tenants、pm-funds + systemd 自愈（Restart=always，kill -9 演练 6s 恢复）
- **数据吞吐**：trades_1s 21MB/47h（164,686 条）、micro_1m 7,146 条/47h、books_1s 已归档 1.4GB gzip
- **风险点**：
  1. 单机无冗余——服务器故障 = 全站停机（systemd 能自愈进程，不能自愈机器）
  2. 内存余量 1.2GB，用户增长后 SSE 连接数线性增长（每连接 ≈ 数 KB，当前量级安全）
  3. 无监控大盘（watchdog 告警靠 TG，无趋势曲线）

**建议（按优先级）**：
1. **每周异地备份**：现备份在本地盘，建议 rsync 到第二节点/对象存储
2. **内存水位线告警**：watchdog 加 free 可用 < 300MB 告警（低成本高收益）
3. 用户数 > 50 后评估升级 2G→4G 内存实例

## 三、合规审查

| 域 | 状态 | 说明 |
|---|---|---|
| 资金安全 | ✅ | 用户资金账本（funds.db）+ 平台统一账户（UTA）；充值匹配、提现审批双人链路 |
| 密钥安全 | ✅ | AES-256-GCM 加密（.dash_secret 派生主密钥）；统一平台密钥 + 90 天轮换告警；用户自助绑密钥已下线（M4） |
| 操作审计 | ✅ | 提现审批/余额调整/费率变更/密钥更新全留痕（users.audit） |
| 数据备份 | ✅ | 每日 SQLite 在线备份 + tenants 归档 + 机密文件（M5 补）；保留 14 天 |
| 实盘风控 | ✅ | 单笔名义/日亏熔断/最大持仓数 per-user（keys.limits）+ 二次确认弹窗 + 平台统一密钥执行 |
| 租户隔离 | ✅ | 13/13 测试；state 按 uid 物理隔离 |
| 法务风险 | ⚠️ 提示 | ①无 KYC/AML 流程（需按经营地法规定制）②无用户协议/风险提示弹窗（建议交易屏首启弹「虚拟资产交易风险提示」）③平台统一账户模式下「用户资金=平台资金」，需在用户协议中明示资金归集方式 |

## 四、路线图终态

- ~~M1 稳定性~~ ✅ ~~M2 现货体系~~ ✅ ~~M3 商业化闭环~~ ✅ ~~M4 统一密钥+同步性~~ ✅ ~~M5 规模合规~~ ✅（本轮）
- **后续观察项**：swing_score 2 周数据复测（9-30 后）｜24h 内存曲线｜账实差异 -80 USDT（用户手动平账中）

## 五、就绪度

**商业化就绪度：9/10**（较 R14 报告 8.5 提升：实盘密钥统一消除多密钥风险、同步性实测保障订单链路、备份补齐机密文件）
扣分项：异地备份缺失（0.5）、法务文档缺失（0.5）。
