# 客服系统开发计划（M-S 系列）

> v1.0 · 2026-09-25 · 商业级设计
> 目标：Web 管理后台 + 用户端双端客服系统——用户可发**图片+文字**与官方客服对话反馈问题

---

## 一、白话理解

**现在**：用户有问题只能靠公告/自己排查，没有官方沟通渠道。
**升级后**：用户在「我的」页点【联系客服】进入聊天窗——像微信一样发文字、发截图；管理员在后台客服台实时看到消息、回复、标记已解决。用户的截图（报错页面/对账单）直接发过去，客服看图定位问题。

## 二、专业级功能清单

### 用户端（移动端 m.html）
| 功能 | 说明 |
|---|---|
| 联系客服入口 | 「我的」页 + 全局入口，带未读红点角标 |
| 聊天窗 | 全屏屏：头部（官方客服 + 在线状态）、消息流、输入栏 |
| 图片发送 | 📎 选择图片（相册/拍照），5MB 内 jpg/png/webp/gif，缩略图显示 + 点击全屏预览 |
| 文字聊天 | 气泡消息（用户右绿/客服左白），时间分组 |
| 未读提醒 | 客服回复 → 入口红点 + 数字角标；打开即已读 |
| 历史记录 | 会话持久化，退出重进不丢 |
| 会话状态 | 看到「处理中/已解决」状态徽标 |

### 管理端（admin.html 新「客服」tab）
| 功能 | 说明 |
|---|---|
| 客服台概览 | 统计卡：总会话/未读/今日新增/待处理/已解决 |
| 会话列表 | 左栏：用户、最后消息、时间、状态色标、未读数 |
| 聊天详情 | 右栏：消息流（用户/客服双侧）、图片点击放大 |
| 回复 | 文字回复 + 图片回复（上传截图给用户看） |
| 快捷回复 | 预置常用语一键插入（如「已收到，正在排查」） |
| 状态流 | 待处理 → 处理中 → 已解决（三态切换，用户端可见） |
| 用户信息侧栏 | 会话用户的 ID/套餐/最近登录，辅助定位问题 |
| 未读聚合 | 客服 tab 角标 = 所有未读用户消息数；新消息实时刷新 |

### 安全与审计
- 图片走鉴权端点（用户只能看自己的图，admin 全量）
- 上传校验：类型白名单 + 5MB 上限 + 随机文件名
- 敏感操作留痕（客服关闭会话/管理员查看）

## 三、数据模型（SQLite，服务器全局库）

```
conversations: id, uid, status(open/pending/resolved), subject,
               last_msg_at, unread_user, unread_admin, created_at, resolved_at
messages: id, conv_id, sender(user/admin), type(text/image),
          content(文字或图片相对路径), created_at, read(0/1)
quick_replies: id, text, sort (客服预置快捷语)
```

- 每用户**一个活跃会话**（有 open/pending 会话时直接续聊，不重复开）——避免会话碎片化
- 已解决后再次发起 → 新会话（历史保留可查）
- 图片存 `data/support_uploads/<ts>_<rand>.<ext>`，DB 存相对路径

## 四、后端 API（FastAPI，全部鉴权）

| 端点 | 说明 |
|---|---|
| `GET /api/support/conv` | 用户：我的会话+消息；admin：会话列表（含未读聚合） |
| `POST /api/support/msg` | 发文字消息（用户/admin 通用，admin 端校验角色） |
| `POST /api/support/upload` | 图片上传（multipart，返回图片 ID+路径） |
| `GET /api/support/img/<id>` | 图片读取（鉴权：本人会话图或 admin） |
| `POST /api/support/status` | admin：改会话状态 |
| `GET /api/support/unread` | 双端未读计数（轮询，15s） |
| `POST /api/support/quick` | admin：快捷回复 CRUD |

**实时方案**：v1 轮询 15s（简单可靠）；SSE 双向推送列入二期（架构预留 event 字段）。

## 五、里程碑 M-S1~M-S4（3 个工作日）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M-S1** 后端+数据 | SQLite 表、7 个 API、图片上传鉴权、未读计数 | curl 全链路（建会话→发文字→发图→admin 读→回图→状态流） |
| **M-S2** 管理端客服台 | admin.html 客服 tab：统计卡+会话列表+聊天详情+快捷回复+状态流 | Playwright：列表/未读/回复/图预览/状态切换 |
| **M-S3** 移动端客服 | 我的页入口+聊天屏+图片发送+未读红点+历史 | Playwright：入口→发文字→发图→收回复→红点消除 |
| **M-S4** 闭环验收 | 双端联调（用户发→admin 收→回复→用户见）、审计留痕、部署 | 双端全路径真机绿 + 图片端到端 |

## 六、UI 交互设计

**用户端聊天屏**（参照 Intercom 移动端精华）：
- 全屏沉浸，头部：官方客服头像（绿色圆形徽）+「官方客服」+ 在线状态点
- 消息流：时间分组（「今天 14:32」居中弱化）、用户消息右对齐绿渐变、客服左对齐暗卡、图片消息圆角缩略图点击全屏
- 输入栏：📎 图片按钮 + 胶囊输入框 + 圆形发送钮（复用 AI 浮窗精修组件语言）
- 状态徽标：「处理中」（黄）/「已解决」（绿）在头部下方

**管理端客服台**（参照 Zendesk/Crisp 后台精华）：
- 顶部统计卡（未读红色强调）
- 左栏会话列表：用户 ID+套餐徽标、最后消息预览、状态色点、未读红点数字
- 右栏对话详情：消息流（用户/客服双侧+图片+时间）、快捷回复下拉、状态三态按钮、输入栏（📎+胶囊+发送）
- 新消息行高亮闪烁 + 自动滚底

设计原型见 `design/support_system_prototype.html`（双画布：手机用户端 + 桌面客服台）。
