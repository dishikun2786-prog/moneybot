# 移动端 AI 策略助手 · 商业级设计与开发计划

> 版本 v1.0 ｜ 2026-09-25 ｜ 基于现状盘点增量规划（后端骨架已具备：ai_client.py 流式工具循环、ai_tools.py 白名单、/api/ai/chat + /api/ai/approve 已上线服务器）

---

## 一、现状盘点（已有的 vs 缺的）

| 层 | 已有 | 缺失 |
|---|---|---|
| 后端 | DeepSeek 流式 + 工具循环（6 轮）、工具白名单分发、审批端点、SSE 转发（X-Accel-Buffering 已处理） | 套餐 gating、定时任务队列、回测数据接口、上下文构建服务 |
| 前端 | —（m.html 零 AI 入口） | 对话 UI、套餐入口、建议卡片、任务卡片、回测卡片 |
| 安全 | 写工具强制审批（三闸） | 套餐级工具分级、用量限额、审计 |

**结论**：后端 50% 就绪，本轮重心 = 前端 UI 交互 + 套餐 gating + 任务/回测能力。

---

## 二、产品定位与功能矩阵

「AI 策略助手」是**付费套餐（专业版/旗舰版）专属**的移动端 AI 交易顾问，四层能力：

| 能力层 | 功能 | 交互载体 |
|---|---|---|
| L1 分析解释 | 持仓风险白话解读、资金费率/基差状态、行情异动点评 | 流式对话气泡 |
| L2 策略建议 | 参数调整建议（swing_score、对冲腿、止盈止损） | 建议卡片 + 预览闸 + 确认 |
| L3 托管决策 | 结合用户持仓+参数+行情给出托管开关/策略切换意见 | 对话 + 一键应用（人审） |
| L4 自动执行 | 智能定时任务（定时检查费率/定时回测/定时提醒） | 任务卡片（启停/日志） |
| 附 回测总结 | 回测数据（swing 26h 等）白话总结 + 关键指标提取 | 回测卡片 + 表格 |

---

## 三、移动端 UI 交互设计（m.html 原生 JS）

### 3.1 入口布局
- **底部 tabbar 保持 5 主 tab 不变**；AI 助手入口放「👤 我的」页顶部第二个卡片（套餐卡之下）。
- 对话页为独立全屏屏（进入后隐藏 tabbar，返回箭头保留）——交易室沉浸式设计。

### 3.2 「我的」页套餐卡片（升级入口，本次一并交付）
```
┌─────────────────────────────┐
│ 💎 当前套餐：专业版（托管30天）    │
│ 到期：2026-10-25 · 托管服务进行中 │
│ 实盘权限：已开通（余额 $120.50）  │
│        [立即升级 →]             │
└─────────────────────────────┘
```
- 点卡片/升级按钮 → 套餐对比弹窗（三档 + 自定义套餐：名称/价格周期/托管时长/特性，数据走 /api/admin/plans 同源）→ 确认升级（走现有充值订阅流）。
- 免费版显示「开通托管服务」引导文案。

### 3.3 对话界面（核心）
- **气泡流**：用户右侧蓝、AI 左侧灰；流式打字机（SSE 分帧逐字渲染，光标闪烁）。
- **思考态**：AI 调工具时显示「🔍 正在查询您的持仓数据…」状态行。
- **建议卡片**（内嵌在 AI 气泡中）：
  - 「调整 swing_score 0.3 → 0.5」+ 理由 + [预览] [应用] 按钮
  - 点预览 → 参数对比弹窗（现值/建议值/影响白话）→ 确认 → 调 /api/ai/approve（人审闸）→ 成功 toast + 成交带留痕
- **快捷指令条**（对话框上方 chips）：「📊 看下我的持仓风险」「💰 今日盈亏总结」「🔄 回测数据总结」「📅 创建定时任务」
- **上下文感知**：每次会话注入 用户持仓/余额/引擎参数/当日台账/费率配置快照（服务端构建，用户不可见）。

### 3.4 定时任务卡片
```
┌─────────────────────────────┐
│ ⏰ 费率监控（每4小时）   ● 运行中 │
│ 下次执行 14:20 · 已执行 12 次    │
│ 动作：检查资金费率 >0.01% 则提醒  │
│ [暂停] [编辑] [执行日志]        │
└─────────────────────────────┘
```
- AI 对话里说「每 4 小时检查资金费率提醒我」→ AI 生成任务描述 + 参数 → 预览卡片 → 确认创建。
- 任务类型（白名单）：费率监控 / 回测运行 / 持仓风险扫描 / 自定义提醒。
- 任务调度：服务端 APScheduler（或自研 tick 循环），状态存 data/ai_tasks.json（租户隔离）。

### 3.5 回测总结卡片
- 「🔄 回测数据总结」→ AI 读取回测文件（swing_score 回测等）→ 输出卡片：总收益/胜率/最大回撤/交易次数 + 白话结论 + 风险提示。

---

## 四、专业提示词设计（核心资产）

### 4.1 系统提示词骨架（存服务端 ai_prompts.py，非前端）
```
你是 moneybot 平台的「AI 交易策略助手」，服务对象是持有{套餐名}套餐的用户。
你的职责分四层：分析解释 / 策略建议 / 托管决策 / 定时任务。

【铁律】
1. 所有回复必须使用简体中文，禁止英文开头。
2. 涉及资金与交易的结论必须先调用工具获取实时数据，禁止凭空猜测。
3. 给出的任何参数调整建议必须附带理由与风险提示，不得承诺收益。
4. 工具调用只使用系统下发的 ASCII 参数键（description 中明写），
   禁止自造键名；写操作必须由用户确认后才生效。
5. 用户是业余投资者，回复用「白话」解释，专业术语后必带括号解释。

【用户实时上下文（每轮自动注入）】
- 持仓：{positions_json}
- 余额/充值状态：{funds_json}
- 引擎参数：{params_json}
- 今日台账：{trades_today_json}
- 平台费率配置：{fee_json}

【可用工具】由服务端按套餐等级下发（见 4.3）。
```

### 4.2 输出格式约定
- 建议类回复结尾必须带 JSON 建议块（前端渲染成卡片）：
  `[SUGGEST] {"action":"set_param","key":"swing_score","value":0.5,"reason":"...","risk":"..."}`
- 任务类：`[TASK] {"type":"fee_watch","interval_h":4,"threshold":0.01,...}`
- 其余为纯白话正文（流式）。

### 4.3 工具分级（套餐 gating，服务端白名单）
| 工具 | 免费版 | 专业版 | 旗舰版 |
|---|---|---|---|
| 行情快照 / 持仓查询（只读） | ✓ | ✓ | ✓ |
| 参数建议（预览闸） | — | ✓ | ✓ |
| 应用参数（人审闸） | — | ✓ | ✓ |
| 定时任务创建 | — | ✓ | ✓ |
| 回测数据读取+总结 | — | ✓ | ✓ |
| 自动策略巡检报告（每日） | — | — | ✓ |

### 4.4 关键坑（来自本平台实测，直接编码规避）
- DeepSeek 流式 tool_calls 按 index 累加、arguments 是未完成 JSON 字符串（拼完再解析）
- 工具输出的键必须 ASCII（中文名放并列字段），否则模型把中文当键名传回被白名单拒绝
- SSE 三件套响应头（text/event-stream + no-cache + X-Accel-Buffering: no）
- 中文提示词下模型仍可能英文开头——容忍，不做语言矫正

---

## 五、技术栈（商业级）

| 层 | 选型 | 说明 |
|---|---|---|
| LLM | DeepSeek V4（deepseek-chat 兼容名） | 已有 key，TTFB 0.7s，1M 上下文 |
| 流式 | FastAPI SSE + starlette iterate_in_threadpool | 已有，复用 |
| 工具层 | ai_tools.py 白名单分发 | 已有，扩展套餐分级 |
| 任务调度 | APScheduler（进程内，随 pm-dash） | 或自研 tick 循环（tenant_scheduler 同款模式，更可控） |
| 任务存储 | data/ai_tasks.json（租户目录） | 与引擎参数同模式 |
| 上下文服务 | ai_context.py（服务端构建，30s 缓存） | 每轮对话拼装用户快照 |
| 前端 | m.html 原生 JS + EventSource + 打字机 | 零新依赖，与全站一致 |
| 审计 | 全量入 users.audit_log（ai_chat/ai_apply/ai_task） | 与三闸同轨 |

---

## 六、开发计划（6 个里程碑，按依赖排序）

### M-A1 套餐入口 + 对话骨架（0.5 天）
- 「我的」页套餐卡片（当前套餐/到期/升级弹窗，数据走 /api/me + /api/admin/plans 只读）
- AI 助手入口 + 对话屏骨架 + SSE 打字机渲染
- 套餐 gating：免费版进对话页显示升级引导，专业版+可用
- **验收**：真机流式对话 10 轮，免费/付费双账户权限正确

### M-A2 上下文注入 + L1 分析（0.5 天）
- ai_context.py 上下文服务（持仓/资金/参数/台账/费率快照）
- 快捷指令「持仓风险」「今日盈亏」白话说人话
- **验收**：AI 回答与用户真实持仓一致（开一仓再问，数字对得上）

### M-A3 L2/L3 托管决策辅助（1 天）
- 建议卡片渲染 + 参数预览弹窗 + /api/ai/approve 人审闸闭环
- 工具分级表落地（免费版只读、付费版可建议）
- **验收**：AI 建议 swing_score → 预览 → 确认 → 引擎参数生效 + 留痕（复用 M5 三闸验收路径）

### M-A4 智能定时任务（1 天）
- ai_tasks.json 存储 + 任务调度循环 + 任务卡片 UI（启停/日志）
- 四类白名单任务：费率监控/回测运行/持仓扫描/自定义提醒
- **验收**：创建「每 4 小时费率提醒」→ 任务真实触发 → 用户收到站内提醒

### M-A5 回测总结（0.5 天）
- 回测数据读取工具 + 总结卡片（指标表格 + 白话结论 + 风险提示）
- **验收**：swing 26h 回测数据 → AI 输出胜率/回撤/结论，数字与文件一致

### M-A6 商业打磨（0.5 天）
- 每日策略巡检报告（旗舰版专属，定时推送）
- 用量限额（每用户每日对话/任务上限，防刷）
- 全量审计 + skill 归档
- **验收**：完整点击路径自检（入口→对话→建议→确认→生效→留痕→套餐卡刷新）

**总计约 4 个工作日**，全部依赖现有 DeepSeek 流式底座与三闸安全体系，无新外部依赖。

---

## 七、商业闭环（为什么值得做）

1. **转化**：免费版 AI 助手「只读试用 + 升级解锁」= 套餐付费的钩子（升级入口就在对话页顶部）
2. **留存**：托管决策辅助 + 定时巡检让用户「每天打开一次」
3. **风控**：所有 AI 建议过三闸，写操作留痕可回退——AI 是参谋不是操盘手
4. **差异化**：行业里「AI 对话」很多，「AI 掌握你实时持仓并辅助托管决策」是 moneybot 特有资产（上下文 = 壁垒）
