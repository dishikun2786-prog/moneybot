# 交易数据资产盘点 × 量化对冲波段策略优化分析

> 2026-09-22 · 基于服务器真实数据盘点（文件规模/字段实测）
> 结论：**量能挂单与大单成交信息已具备且实时可用**，但存在 5 个关键缺口，补齐后可将当前「regime 级对冲」升级为「微结构择时波段对冲」。

## 一、现有数据资产盘点（实测）

| 数据 | 文件/源 | 规模 | 粒度 | 波段可用性 |
|---|---|---|---|---|
| 永续+现货实时价 | bybit_prices.json | 100ms | tick | ✅ 已用 |
| **盘口 200 档挂单** | orderbook.json（1s 快照） | 87KB/帧 | 1s | ✅ 挂单墙/深度分布可用 |
| **逐笔成交（含方向）** | 内存环 30 笔 + trades_1s.jsonl | **18.7 万行** | 1s 聚合 | ✅ 主动量失衡/买卖比可用 |
| **三窗口主动量聚合** | orderbook.json aggs | 15s/60s/300s×档位 | 1s | ✅ 已上盘口视图 |
| **大单事件** | big 环形（仅 12 条内存） | ⚠️ **无持久化** | 实时 | ⚠️ 历史大单丢失 |
| K线全周期 | kline_1m~M（8 数据集） | 千万行级 | 分钟~月 | ✅ |
| funding/基差 | carry_1m.jsonl | 31 万行 | 1m | ✅ 套利核心 |
| **未平仓量 OI** | ✗ **未采集**（Bybit ticker 自带 32 字段含 openInterest/openInterestValue，桥只存了 6 个） | — | — | ❌ 缺口 |
| **CVD 累积成交量差** | ✗ 未计算（原料 trades_1s 已有） | — | — | ❌ 缺口 |
| 挂单墙/撤单行为 | ✗ 未检测未留痕 | — | — | ❌ 缺口 |

## 二、波段信号需求 × 数据匹配（gap 分析核心）

| 波段策略信号 | 需要的数据 | 现状 | 判定 |
|---|---|---|---|
| 主动量失衡（taker 买卖比） | 逐笔方向+量 | ✅ 有 | 可直接用 |
| **挂单墙检测**（单档巨量挂单=支撑/压力） | 盘口各档 size | ⚠️ 原始数据有，**检测器未建** | 低成本可补 |
| 墙的堆积/崩塌（吸筹/出货） | 盘口历史帧对比 | ⚠️ 仅 1s 快照且不留帧史 | 需加帧史留痕 |
| **CVD 背离**（价涨量差跌=动能衰竭） | 逐笔量差累计 | ⚠️ 原料有，未累计 | 纯计算可补 |
| **OI 变化率**（增仓/减仓方向） | OI 时间序列 | ❌ 未采集 | 桥改一行 |
| 大单统计（历史分布/方向） | 大单事件流 | ❌ 无持久化 | 加 jsonl 留痕 |
| 闪单/撤单率（spoofing） | 盘口高频帧 | ❌ 1s 太粗 | 需 100ms 帧采样 |
| 单笔量分布（大中小单结构） | 逐笔 v | ✅ 有（未统计） | 纯计算可补 |
| 现货-永续基差节奏 | carry_1m | ✅ 有 | 已用 |

**核心结论**：主动量失衡、挂单墙、CVD、OI、大单结构——**波段择时所需的五大微结构信号，三项已有原料只差计算，两项需极小采集改动，仅闪单检测需要中等改造**。

## 三、专业优化方案（P0→P2）

### P0 — 纯计算/一行改动级（~120 行，1 轮）

1. **ticker 补存 OI**：桥 ticker 分支 `.update` 增 `oi`/`oi_val` 两键 → 入 price_1s.jsonl 与 DuckDB。OI 变化率是波段最核心的仓位方向确认信号之一。
2. **CVD 累积差**：trades_1s 每条增 `cvd = bv − sv` 与 `cum_cvd`（当日滚动累计），前端盘口视图加 CVD 曲线（价格 vs CVD 双轴，背离一目了然）。
3. **挂单墙检测器**：orderbook 快照写入时检测：单档 size ≥ 同侧前 20 档均值×8 或 ≥ 该侧总量 25% → 记 `wall_events.jsonl`（价/量/侧/时间），盘口视图该档**加粗+「墙」徽标**。
4. **大单历史流**：BIG 环形 → `big_trades.jsonl` 持久化，供事后复盘与统计。

### P1 — 信号指标层（~200 行）

5. **微结构指标包**（入 DuckDB，供 AI 助手/回测）：
   - 主动买卖比、平均单笔量、大小单占比（按单笔量分位）
   - 1m CVD 斜率、OI 5m 变化率
   - 挂单墙命中率（墙被击穿 vs 守住的统计）
6. **盘口帧史采样**：100ms 采样 book 中 10 档（只存档头，量小）→ 撤单率/闪单粗检测。
7. **量价分布持久化**：±1% volume profile 按小时归档 → 关键价位（POC/VAH/VAL）识别。

### P2 — 引擎级接入（策略联动）

8. **波段信号合成评分**（0-100）：`0.4×主动量失衡 + 0.3×CVD 斜率 + 0.2×墙方向 + 0.1×OI 变化` → 用于：
   - **入场择时**：正向套利窗口期，评分 <40 时延迟入场（等墙确认）；>70 时立即入场
   - **持仓风控**：评分急跌（5 分钟 >30 分）→ 预警（TG 推送 + 看板横幅），提示检查裸腿止损
   - **加仓/减仓**：评分与持仓方向同向且墙重构 → 半仓加码；反向 → 提前平合约腿
9. **回测验证**：用 trades_1s/price_1s 历史（已积累）回测「裸信号 vs 加信号」的胜率/年化提升，达标才正式接入引擎（沿用现有参数审批三闸）。

## 四、波段策略框架（白话）

现在的对冲是「看 funding 水位进出场」（**regime 级**，周期=天）。加入微结构信号后升级为「**regime 定方向 + 微结构择时点**」（**波段级**，周期=分钟~小时）：

```
funding 正向 regime (应持有正向套利)
   ├─ 微结构共振(主动买+CVD升+买墙厚) → 入场/加仓
   ├─ 微结构背离(价涨但CVD跌+卖墙堆积) → 减仓/收紧裸腿止损
   └─ 大单砸盘+墙崩塌 → 平合约腿锁利, 现货转孤儿等信号恢复
funding 负向 regime (反向套利)  → 完全对称
```

一句话：**funding 告诉你「该做什么方向」，挂单/成交微结构告诉你「现在是不是动手的好时点、还是再等等」**——这正是大资金做市商的经典两段式：方向来自成本/资金，时点来自盘口。

## 五、实施路线（确认后开工）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M1 数据补齐 | ticker OI + CVD + 墙检测器 + 大单留痕（~120 行桥/引擎改动） | wall_events/big_trades 落盘，CVD 曲线正确 |
| M2 指标层 | DuckDB 微结构指标包 + readers API（~200 行） | AI 助手可查微结构指标 |
| M3 波段视图 | 盘口视图增 CVD 曲线/墙徽标/OI 面板（~150 行） | 墙标注与数据一致 |
| M4 策略接入 | 波段评分 + 回测验证 + 引擎择时接入（三闸审批） | 回测报告 + 参数化接入 |

预估 2-3 轮交付，全部沿用现有架构（桥/SSE/DuckDB/参数审批），无新技术栈。
