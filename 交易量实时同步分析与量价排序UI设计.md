# Bybit 交易量实时同步分析 + 卖方/买方量价排序 UI 交互设计

> 2026-09-22 · 基于真实探测数据（tools/probe_bybit_volume.py，都柏林→Bybit WSS 实测）
> 结论先行：**完全支持实时同步展示**，且 Bybit 独有的 side 字段让「主动买/主动卖」无需推断即可直出。

## 一、可行性分析（实测证据）

### 1.1 探测结果（15.2 秒实测，BTC+ETH 双标的）

| 通道 | 推送速率 | 关键字段 | 结论 |
|---|---|---|---|
| `orderbook.200` | **21.4 条/s**（快照2+增量323，增量均值60档） | b/a 数组（价+量）、u 序列号、type=snapshot/delta | ✅ 盘口挂单量实时可得 |
| `publicTrade` | **14.1 条消息/s（≈57 笔成交/s）** | **`S`=Buy/Sell（主动方向）**、v 量、p 价、T 时间戳 | ✅ 主动买/卖量直接可得 |
| 时延 | min **82ms**（与现网桥 84-94ms 稳态一致） | — | ✅ 同步级实时 |

### 1.2 三个关键结论

1. **买方/卖方量价排序有两条真实数据源**：
   - 挂单量（resting）：orderbook 深度快照+增量 → 「卖方挂单阶梯 / 买方挂单阶梯」
   - 主动成交量（taker）：publicTrade 逐笔 → 「主动买量 / 主动卖量」按价位聚合
2. **Bybit v5 publicTrade 自带 `S`（side）字段**——不像币安需要 tick-rule 推断，每笔成交的主动方向是交易所直接给的，数据无歧义。
3. **现有架构零障碍复用**：桥已维护 WSS 连接（84ms 稳态），只需在**同一连接**加订 4 个 topic（book×2 + trade×2），不改连接模型、不动 SSE 认证体系。

### 1.3 诚实的边界

| 边界 | 说明 | 处理 |
|---|---|---|
| 挂单可撤 | 挂单量≠真实意愿（可撤单），主动成交量才是真金白银 | UI 两者分列，主动量标注「真实成交」 |
| 深度范围 | 200 档仅覆盖 BTC ±20$/ETH ±2$（约±0.02%~0.07%） | 阶梯+分布图以 ±1% 为展示窗；远档用 500 档可扩 |
| 涨跌方向≠主动方向 | 红涨可能伴随主动买 | 颜色语义用「主动买/卖」而非涨跌，避免误导 |
| 数据量 | book 全量 400 档×2 标的 ≈40KB/s + 成交聚合 ≈5KB/s | 1s 节流推送，SSE 带宽毫无压力 |
| 永续 vs 现货 | 两套 book 不同，我们交易的是永续 | 只用**永续** book（linear 通道）|

## 二、数据管道设计（三层复用现网架构）

```
Bybit WSS ──单连接──▶ bybit_ws_bridge.py ──SSE──▶ 浏览器 EventSource
  orderbook.200 ×2   │  新增: book 状态机(快照+u序列增量重组)   │  1s 节流
  publicTrade ×2     │  新增: 逐笔环形缓冲(最近200笔)          │  登录态
                     │  新增: 价位聚合器(档位可调)             │  心跳/断连
                     ▼
          logs/orderbook.json (原子写快照)
          logs/trades_1s.jsonl (秒级聚合 → DuckDB 管道扩展)
```

- book 状态机：snapshot 全量 + delta 按 `u` 序列号连续重组（乱序丢弃），每 1s 原子落盘
- 价位聚合：BTC 默认档 1$、ETH 0.1$（可切换 0.1/1/10/100 档）；每价位累计「主动买量/主动卖量/笔数」
- 大单检测：单笔 ≥ 阈值（BTC 5 / ETH 50，可配）→ 事件标记随 SSE 下发
- SSE：复用现有 `/api/stream/prices` 登录态+心跳+断连检测，新增 `/api/stream/depth` 或并入现有流

## 三、UI 交互设计（交易室新增「盘口」视图）

### 3.1 整体布局：量价排序阶梯（核心）

```
┌───────────── 卖方（主动卖出=价格承压） ─────────────┬──────────── 买方 ─────────────┐
│ 价格      挂单量  主动卖量  累计量   [深度条]          │ 87,215.0 ▲ 最新价跳动        │
│ 87,214.0  12.4    8.1     65.3    ████████░░ (红)    │ [深度条] 累计量 主动买量 挂单量 价格 │
│ 87,213.0  3.1     0.9     52.9    ███░░░░░░░ (红)    │ ████████░░ (绿) 28.4  15.2  9.9  87,212.5 │
│ 87,212.5  40.0◈   2.2     49.8    ██████████ (金=大单)│ ████░░░░░░ (绿) 16.8  6.1   5.3  87,212.0 │
│   ⋮                                                │   ⋮                                    │
└────────────────────────────────────────────────────┴──────────────────────────────────────┘
    [排序: 价格▾] [档位: 1$▾] [主动量窗口: 1分钟▾]   [买卖失衡: 主动买 75.7% ████████░░ 主动卖 24.3%]
```

**每一价位行（两侧对称，5 个字段）**：
1. **价格**（大字等宽）
2. **挂单量**（被动委托，灰白）
3. **主动成交量**（该价位真实成交，红=主动卖/绿=主动买）
4. **累计量**（从最新价累加，显示深度梯度）
5. **深度条**（宽度∝累计量，视觉化「墙」的厚度）

### 3.2 核心交互：四种排序模式（本设计要点）

| 排序模式 | 语义（白话） | 用途 |
|---|---|---|
| **按价格**（默认） | 盘口原貌，卖在上买在下 | 看支撑/压力位形态 |
| **按挂单量** | 谁挂得最多排前面 | 找「挂单墙」——大额被动单聚集位 |
| **按主动成交量** | 哪个价位真金白银成交最多排前面 | 找「真实战斗位」——多空真正交火的价格 |
| **按净主动量** | 主动买−主动卖，看谁在主导 | 找多空主动权分界线 |

排序切换即时重排（无请求，前端内存排序），当前排序模式下**默认展示前 20 位**，可展开全部 400 档。

### 3.3 辅助组件

| 组件 | 设计 |
|---|---|
| **买卖失衡条** | 实时「主动买量 : 主动卖量」比率条（1s 更新），>65% 单向时该侧金色提示「买方主导/卖方主导」 |
| **大单雷达** | 单笔≥阈值 → 该价位金色闪烁 1.5s + Toast「BTC 大单: 主动买 12.4 @ 87,214」+ 可选提示音 |
| **成交明细带** | 底部横流，最近 200 笔逐笔：时间/价格/量/方向色块（绿=主动买 红=主动卖），新笔左侧插入+闪烁 |
| **量价分布图** | ECharts 直方图：当前价 ±1% 范围按档位聚合的成交量分布（主动买/卖双色柱），30s 刷新 |
| **白话解释区** | 折叠面板：「挂单量=还没成交的委托，可撤销；主动成交量=已经成交的真钱——两者结合看主力意图」 |

### 3.4 实时性与性能规格

- 阶梯/失衡条/成交带：**1s 全量刷新**（SSE 推送）；最新价沿用现有 100ms 实时价
- 渲染：400 档 × 2 标的直接 DOM 渲染（无虚拟滚动必要）；价格/量/累计用 `textContent` 定点更新，深度条用 `transform: scaleX`（GPU 友好，避免重排）
- 档位切换/排序切换/窗口切换：纯前端内存重算，0 网络请求，<10ms 响应
- 移动端（375px）：阶梯改为**单侧折叠**（默认卖方，可切买方）、行高 ≥44px、排序/档位进底部 sheet

### 3.5 与其他页面联动

- 交易室持仓卡片已有点位展示，盘口视图的「主力价区」可叠加标注持仓入场价（虚线）
- 手动交易票增加「盘口速览」小窗（点击挂单价即填入开仓价）——二阶段可选

## 四、实施里程碑（确认后开工）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M1 桥扩展 | book 状态机+逐笔环形+价位聚合+大单检测+快照落盘（~150 行） | 快照 400 档完整、u 序列连续、聚合数=逐笔数 |
| M2 SSE | /api/stream/depth（book+聚合+大单事件，1s 节流） | 直连 5s 内收到 5 帧且买卖量与探测一致 |
| M3 UI | 盘口视图：双侧阶梯+4 排序模式+档位/窗口切换+失衡条+成交带+分布图（~300 行） | 排序切换 <10ms、1s 跳价、大单闪烁 |
| M4 验收 | 移动端适配+性能观测+与实盘数据交叉核对（book 中价≈实时价） | 375px 全可操作、SSE 断连自恢复 |

预估 ~500 行，2 轮交付。探测脚本已入库 `tools/probe_bybit_volume.py` 可复跑。
