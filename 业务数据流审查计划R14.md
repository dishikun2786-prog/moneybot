# 业务数据流完整性全面审查计划 R14

> 背景：连续出现「买多后持仓不可见」（原生仓/套利仓渲染分离断链）、「K线缩放失效」（数据量变化引发参数失效）。
> 这两类 BUG 的共同根因：**前端渲染源与后端数据源不同构**、**参数对数据量/结构的隐性依赖**。
> 本计划按数据流全链路逐段审查，每段产出：现状→风险点→验收标准。

## 审查原则
1. 每个「业务动作」必须能回答：**谁写入了什么 → 谁读取 → 谁渲染 → 渲染是否与写入同构**。
2. 前端渲染源一律以 API 实测响应形状为准（TestClient + 真机 Playwright 双验证），不信任代码注释。
3. 每修复一处断链，必须补一条防回归断言（tests/ 或 smoke 脚本）。

---

## 阶段 A：交易全链路（开仓→持仓→平仓→结算）【P0，本轮已完成 2 项修复】

### A1. 开仓写入 vs 持仓读取一致性
- **审查点**：`/api/native/open` 写入 CARRY_STATE native 键 → `/api/native/positions` 读回 → 前端两处渲染（nt-pos 下单tab / pos-list 持仓tab）
- **已发现问题**：原生仓只渲染 nt-pos，持仓 tab 看不到 → **已修复**（loadPositions 合并渲染）
- **待查**：
  - open_native 写 state 是同步落盘还是异步（服务重启丢单风险）
  - 开仓后 `/api/pnl` 的 positions 与 native 仓是否会**重复计入**（套利引擎扫描时 native 仓是否被当作裸仓/孤儿仓触发策略）
  - 纸面 open_spot 现货仓是否同样渲染分离（现货单在持仓 tab 可见性）
- **验收**：开 native/spot/carry 三类仓各一笔 → 持仓 tab 三类全可见且不重复 → 重启 pm-dash 后仍在 → 平仓后全消失

### A2. 平仓全链路
- **审查点**：close_native / close_spot / close_perp_leg / close_both / close_orphan / close_naked → PnL 结算写入 → state 清理 → 前端持仓/余额刷新
- **待查**：每类平仓的 PnL 是否入账（funds journal）、状态文件残留、前端 toast 后 5s 轮询是否覆盖所有持仓区
- **验收**：六类平仓各一笔，比对「平仓前市值 - 平仓价差额」与 journal 入账一致（误差 < 0.001$）

### A3. 资金链路
- **审查点**：/api/funds/balance、充值记录、提现（500 USDT 限额）、纸面资金变动（open 扣减/close 回补）
- **待查**：开仓不实际扣减纸面余额（仅名义占用）是否与 UI 显示一致；MTM 与 PnL 口径是否统一
- **验收**：test_funds 41/41 回归 + 新增「开仓→余额不变、平仓→盈亏入账」断言

## 阶段 B：行情数据流（实时性/一致性）【P1】

### B1. 四标的 SSE 全链路
- **审查点**：bybit_ws_bridge → prices.json/orderbook.json → /api/stream/prices(100ms) + /api/stream/depth(1s) → 前端渲染
- **待查**：SSE 断线重连后数据时效标记（stale 徽标）、depth 与 prices 两流的时间戳一致性、orderbook trades 30 条截断是否丢失大单
- **验收**：断网 30s → 恢复 → 前端 3s 内回到实时且无旧价残留

### B2. K线多周期一致性
- **审查点**：kline.1 实时订阅 + parquet + REST 回源三源合并（klines 优先级①订阅缓存②parquet③REST）——**三源切换时末根是否连续、是否重根/缺根**
- **待查**：W/M 周期数据源新鲜度（周线 29 根/月线 7 根是否滞后）；klines 缓存 TTL 60s 内价格漂移
- **验收**：相邻两次请求（间隔 5s）同一周期根数单调不减；三源边界末根 OHLC 无跳变

## 阶段 C：模式与白名单【P2】

### C1. 托管/手动双模式
- **审查点**：engine_mode.json 两策略独立 → swing/carry 引擎实际生效模式 → 前端 MODES 显示与后端一致
- **待查**：手动模式下 carry 引擎是否仍会触发开仓（越权）；AI 审批流与手动切换并发冲突
- **验收**：手动模式运行 10 分钟 → 审计日志 0 条引擎开仓记录

### C2. 标的白名单防回归
- **审查点**：SYM_WHITELIST 4 标的在 bridge/API/前端三处一致（test_symbols_wl 3/3 已有）
- **待查**：/api/instruments 过滤后详情屏/下单 tab 是否残留 4 标的外引用
- **验收**：白名单回归测试全绿 + 前端无 4 标的外标的任何可下单入口

## 阶段 D：多租户与权限【P2】

### D1. 租户隔离
- **审查点**：所有 state 读写是否带 uid；admin 查看用户持仓是否走列表聚合而非直接读用户文件
- **待查**：跨用户平仓/开仓接口是否有 uid 校验（**越权高危**）
- **验收**：用户 A token 调用户 B 的持仓/平仓接口 → 403

### D2. 密钥与审批
- **审查点**：平台密钥 AES-256-GCM 存储、AI 三闸审批流、admin 审计留痕
- **验收**：审计日志覆盖所有变更类操作（开/平/提现/密钥/模式切换）且含操作者 uid + 时间

## 阶段 E：稳定性【P3】

### E1. 内存与 OOM 回归
- **审查点**：swap 生效、paper_engine 尾部读、data_writer 峰值、books_1s 归档完成情况
- **验收**：连续 24h 内存 < 1.2GB；watchdog 无 PM-OFF 误报

### E2. 服务自愈
- **审查点**：watchdog 300MB 阈值、pm-dash/pm-bridge 崩溃重启、断线重连
- **验收**：kill -9 pm-dash → watchdog 90s 内拉起 → 外网恢复 200

---

## 执行顺序与交付物
1. **本轮已交付**：A1 两项修复（缩放 + 原生仓并入持仓 tab），真机验证通过
2. **下一步建议**：按 P0→P1→P2→P3 顺序执行 A2/A3 → B1/B2 → C/D → E
3. **每阶段交付**：修复 + tests/ 断言 + 真机验证截图/输出 + commit
4. **总验收**：test 全家桶全绿 + 一条「开仓→持仓→平仓→入账」端到端自动化脚本（e2e_full_cycle.py）

## 预估工作量
- A2+A3：0.5 天（六类平仓一致性 + 资金口径）
- B1+B2：0.5 天（SSE 时效 + 三源 K线）
- C+D：0.5 天（越权校验最关键）
- E：0.5 天（稳定性观察）
- 合计约 2 天，逐阶段确认
