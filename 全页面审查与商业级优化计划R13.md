# 移动端全页面审查与商业级优化开发计划（R13）

审查日期：2026-09-23 ｜ 范围：移动端 /m 十屏 + 管理后台 + 数据链路
现状盘点基于代码与实测；每项附优先级、工作量、收益。

---

## 一、审查发现（现状 vs 问题）

### A. 数据实时展示（6 项）
| # | 现状 | 问题 | 严重度 |
|---|---|---|---|
| A1 | 行情列表：SSE 300ms 推送 + diff 合并 | ✅ 良好 | - |
| A2 | K线末根：**15s 轮询** refreshKline | 交易决策时 15s 陈旧 | 中 |
| A3 | 持仓 MTM：15s 轮询 loadPositions | 交易屏盯盘时浮盈滞后 | 中 |
| A4 | 盘口：watch 订阅 + 3s 轮询 | ✅ 良好 | - |
| A5 | 资产屏：15s 轮询 loadAssets | 充值到账后最长 15s 才见 | 低 |
| A6 | SSE 断线：es.onerror 有状态显示 | 需确认移动端展示位置明显度 | 低 |

### B. 加载同步性能（5 项）
| # | 现状 | 问题 | 严重度 |
|---|---|---|---|
| B1 | PM 首帧 154KB 分页懒加载 + 骨架屏 | ✅ 良好（R8） | - |
| B2 | 管理后台用户列表：**全量加载无分页** | 用户量增长后首屏卡顿 | 中 |
| B3 | 详情屏打开：kline+盘口 watch 串行触发 | 可并行预取（K线+REST 同时） | 低 |
| B4 | 登录屏验证码：双图（login+reg）同时拉 | 注册屏未显示也拉，浪费一次请求 | 低 |
| B5 | 首屏 boot：/api/auth/me → 行情 SSE → PM 分页 串行 | 已可接受；SSE 首帧 Top120 先行 ✓ | - |

### C. UI 交互商业级（6 项）
| # | 现状 | 优化空间 | 严重度 |
|---|---|---|---|
| C1 | 验证码加载失败仅 toast | 无「点击重试」视觉反馈 | 低 |
| C2 | 无下拉刷新 | 移动端原生习惯缺失 | 中 |
| C3 | 关注列表删除需进详情屏 | 列表长按/滑动删除更顺 | 低 |
| C4 | 空态（无持仓/无关注）无引导 | 空白区应给文案+行动按钮 | 低 |
| C5 | 价格闪烁 vflash 已有 | ✅ 良好（R 轮） | - |
| C6 | toast 单一色 | 成功/失败/加载三态区分度不够 | 低 |

### D. 数据一致性/健壮性（3 项，本次 BUG 根因所在）
| # | 现状 | 问题 | 严重度 |
|---|---|---|---|
| D1 | **API 响应 ok 字段不统一**（stats/audit/users 无 ok，funds/pm 有） | R12f 已修前端，但**后端应统一**杜绝再犯 | 高 |
| D2 | 前端判断风格混杂（status===403 / !data.ok / !data） | 应统一 api() 包装层处理 | 中 |
| D3 | 数字精度显示：PM 价格 ¢ 与 $ 混排场景多 | 单位标签（¢/$/股）应全站统一标注 | 低 |

---

## 二、开发计划（P0 → P3，按序实施）

### P0：后端 API 响应标准化 + 前端判断统一（根除 D1/D2 类 BUG）
1. main.py 所有 /api/admin/* 端点补 `ok:true`（stats/users/audit/limits）
2. 前端统一：`api()` 内 401→登录屏、403→toast 权限；页面层不再重复判断 ok/status
3. 回归：smoke_m_admin + smoke_admin_html + 全量后端测试

### P1：数据实时性提升（A2/A3/A5）
1. K线末根轮询 15s→**3s**（详情屏激活时；后台屏降频 30s，省流量）
2. 持仓 MTM：交易屏/资产屏激活时 15s→**5s**，离开屏幕停轮询（`curScreen` 判断已有基建）
3. 资产屏在「充值中」状态时 5s 轮询，否则 30s
4. SSE 连接状态徽标（行情屏顶部小圆点：绿=实时/黄=重连中/红=离线降级轮询）

### P2：商业级 UI 交互（C1-C6 + B2）
1. **下拉刷新**：行情/资产/PM 三屏支持原生下拉（touch 手势 + 顶部提示条）
2. **管理后台用户列表分页**（50/页 + 搜索防抖已有）
3. **空态引导**：无持仓→「去交易」按钮；无关注→「去行情逛」；无审计→说明文案
4. **验证码点击重试**：图片加载失败显示「点击重试」占位（onerror 处理）
5. **toast 三态**：成功绿✓/失败红✗/加载 spinner（现有 toast 升级）
6. **关注列表长按删除**（longpress 500ms 弹操作菜单）
7. 登录/注册屏验证码懒加载（切到对应屏才拉）

### P3：性能与细节打磨（B3/B4/D3）
1. 详情屏打开并行预取（K线 REST + 盘口 watch 同时发）
2. PM 价格单位标注统一（买价¢、成本$、回报$、股数）
3. 交易成功后的成交留痕动画（现有留痕区 vflash 强化）
4. 全站体检：冒烟 4 件套（m_front/m_admin/admin_html + 后端回归）

### 验证策略（每阶段）
- 前端冒烟三件套：smoke_m_front（18）+ smoke_m_admin（7）+ smoke_admin_html（8）
- 后端回归：e2e_p1/funds/e2e_funds/native/spot/pm_units/pm_admin
- 部署后真实浏览器几何/交互验证 + 服务器 API 实测
