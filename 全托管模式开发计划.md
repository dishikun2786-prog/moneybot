# AI 策略助手全托管模式开发计划（M-P 系列）

> 版本 v1.0 · 2026-09-25 · 商业级设计
> 目标：用户一次授权 → AI 完全自主的全策略托管与交易决策；提供托管任务查看/取消/修改页面；支持单标的与多标的两种托管粒度

---

## 一、白话理解全托管

**现状**（对话式执行，M-D4）：AI 每笔交易建议都要用户点【批准】——人盯盘，AI 是参谋。

**全托管**（本期）：用户**一次性授权**后，AI 在授权范围内**自主决策自主执行**——人只做三件事：开托管、看托管、停托管。

打个比方：现在是你和 AI 双人开车（AI 报路况你打方向盘），全托管是切换成 L3 自动驾驶（AI 自己开），但**刹车和紧急停车永远在人手里**——一键暂停。

## 二、核心铁律（商业级安全边界）

1. **托管≠裸奔**：跳过的是「逐笔人审闸」，风控闸全部保留并**加码**——单标的仓位上限、总仓位上限、每日最大亏损熔断、资金费率翻转保护、紧急一键暂停。
2. **授权分级**（用户授权时自主选择）：
   - A 级：AI 自主交易决策（开/平/加减仓自动执行）
   - B 级：A + Jev 门控参数自动微调（白名单范围内，如 open_p/risk_pause）
   - C 级：A+B + DeepSeek 战略参数（θ/标的池）自动调整——**默认不开放**，需额外确认
3. **范围封闭**：托管只允许在双通道套利池（BTC/ETH/XAUT/SOL/NEAR/XRP）内做 carry 策略动作；名义金额有上下限；永不触碰用户余额以外的资金。
4. **全量留痕**：托管执行的每一笔决策（信号→风控→执行→结果）全量审计，可回放、可追溯、可对账。

## 三、业务数据流设计

```
┌─ 托管授权（一次性，分级 A/B）────────────────────────┐
│  授权弹窗: 风险条款 + 分级选择 + 风控参数确认          │
└──────────────────┬──────────────────────────────────┘
                   ▼
┌─ 托管任务（autopilot 任务模型，租户级）───────────────┐
│  { id, scope: all | [标的...], notional, θ,           │
│    max_positions, daily_loss_cap, freq,               │
│    auth_level: A/B, status: running|paused }          │
└──────────────────┬──────────────────────────────────┘
                   ▼
┌─ 决策引擎（复用 Jev 5分钟周期，jev_engine.run_cycle）──┐
│  state快照 → 批量判断(开仓noul/风险score/最佳choice)   │
│  → 门控(open_p/把握度/risk_pause)                     │
└──────────────────┬──────────────────────────────────┘
                   ▼
┌─ 托管执行器（新增 autopilot.py，核心）─────────────────┐
│  信号进入 → 逐项过风控闸:                              │
│  ① 授权有效(status=running, 套餐未到期)               │
│  ② 标的白名单(scope 内) ③ 单标的仓位上限              │
│  ④ 总仓位上限 ⑤ 日损熔断(daily_loss_cap)              │
│  ⑥ 费率翻转保护(funding 转负禁开) ⑦ MIN_ORDER/余额    │
│  全过 → 自动执行 open_hedge / close_both              │
│  任一闸不过 → 留痕跳过(原因可查)                      │
└──────────────────┬──────────────────────────────────┘
                   ▼
┌─ 执行与留痕 ─────────────────────────────────────────┐
│  paper_ops / live_exec 真实成交                       │
│  → autopilot_actions.jsonl (信号/闸门/执行/结果)      │
│  → 托管任务卡实时状态(持仓/盈亏/最近动作)             │
│  → 营收计费(M14 费率体系自动生效)                     │
└─────────────────────────────────────────────────────┘
```

**与 DeepSeek 巡检的联动**：1 小时巡检发现异常（费率 regime 恶化、波动率暴增、回测健康度衰减）→ 若授权 B 级：Jev 门控参数自动微调（白名单内）；若需战略级调整（θ/标的池）→ 生成托管报告提示用户（C 级用户自动生效）。

**与现有对话式执行的关系**：全托管与对话式执行并存——托管运行中，用户在 AI 对话里说「暂停 BTC 托管」「修改托管名义到 20U」→ AI 走托管任务工具（修改托管=变更类，仍需人审一次）；未托管的标的对话式执行照旧。

## 四、UI 交互设计（移动端托管中心）

### 4.1 入口
- AI 策略助手屏顶部加「🤖 托管中心」入口卡（显示运行中任务数）
- 我的页套餐卡下方加托管状态徽标（运行中 N 个）

### 4.2 托管中心页（新屏，沉浸式无 tabbar）

```
┌─────────────────────────────────┐
│ ← 返回     🤖 AI 托管中心       │
│                                 │
│ [全托管总开关]  ○ 已授权(分级B) │  ← 一键全部暂停
│ 授权条款 · 分级 · 风控参数      │
│                                 │
│ ┌─ 托管任务卡 ────────────────┐ │
│ │ ●运行中  全策略托管(6标的)   │ │
│ │ 持仓 2/3 · 今日 +1.25$       │ │
│ │ 名义 15U/标的 · θ=5%         │ │
│ │ 最近: 12:05 XRP 开仓 ✓       │ │
│ │ [暂停] [修改] [取消]         │ │
│ └─────────────────────────────┘ │
│ ┌─ 托管任务卡 ────────────────┐ │
│ │ ●运行中  单标的托管 BTC      │ │
│ │ 无持仓 · 今日 0.00$          │ │
│ │ [暂停] [修改] [取消]         │ │
│ └─────────────────────────────┘ │
│                                 │
│ [＋ 新建托管任务]               │ ← 多标的/单标的选择
└─────────────────────────────────┘
```

### 4.3 新建托管（两种路径）
- **AI 对话**：「帮我全托管 6 个标的」/「托管 BTC 一个标的」→ AI 理解 → 生成托管任务预览（范围/名义/风控/授权分级）→ 用户确认 → 任务运行
- **托管中心直接建**：新建 sheet 选类型（全策略/单标的）→ 选标的 → 名义/θ/风控参数 → 授权分级 → 授权条款勾选 → 开通

### 4.4 任务操作
- **暂停/恢复**：单任务级开关（不影响其他任务）
- **修改**：改名义/θ/风控参数（修改类仍走人审确认一次）
- **取消**：删除任务（关闭该标的所有托管持仓动作，**持仓可选择自动平仓或保留由用户手动处理**）
- **实时状态**：轮询/SSE 更新持仓、盈亏、最近动作、运行时长

### 4.5 授权弹窗（一次性，商业级）
明示条款：①AI 将自主开平仓，盈亏由用户承担 ②风控参数展示（上限/熔断线）③分级选择（A/B）④「我已知晓风险」勾选 + 确认按钮。授权状态持久化，可随时撤回（撤回=所有托管任务暂停）。

## 五、业务逻辑关键点

| 逻辑 | 设计 |
|---|---|
| **托管粒度** | 全策略任务（标的集）+ 单标的任务，可多任务并存；同标的冲突时以先创建任务为准（后建任务该标的自动排除，提示用户） |
| **风控七闸** | 授权→白名单→单标的上限→总上限→日损熔断→费率翻转→余额/MIN_ORDER；任一不过→留痕跳过并计数（连续跳过 N 次提示） |
| **日损熔断** | 托管日亏损≥上限 → 全部托管任务自动暂停 + 站内/微信告警 + 次日 0 点（UTC）手动或自动恢复（用户可选） |
| **费率翻转保护** | funding 转负 → 立即平该标的托管仓（不只是禁开） |
| **计费** | 托管成交走现有 M14 计费（官方费×VIP×标的倍率+点差），营收自动入账 |
| **套餐 gating** | 全托管对所有付费套餐开放（套餐只区分服务时长，与每日巡检一致）；免费用户只读托管中心（引导升级） |
| **成本控制** | 托管用户的 Jev 决策频率可配置（5/15/30 分钟），随任务参数；无持仓时降频至 15 分钟 |

## 六、商业级工程要求

- **审计面板**：admin 营收大盘「AI 决策面板」扩展托管维度——运行任务数/托管成交/托管盈亏/闸门拦截率/熔断事件
- **告警**：熔断、连续拒绝、API 降级 → 站内 + 现有 TG 告警通道
- **回滚**：托管参数变更走审计；取消托管可一键平仓
- **合规文案**：全托管不等于保本——UI 明示「AI 托管有亏损风险，平台不承诺收益」（与现有 AI 风险提示一致）

## 七、里程碑（估算 3.5 个工作日）

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M-P1 托管任务模型+授权**（0.5天） | autopilot.py：任务 CRUD（scope all/单标的、notional、θ、风控参数、auth_level、status）+ 授权状态存储 + API（list/create/pause/resume/modify/cancel/authorize） | 任务 CRUD 全链路 API 测试通过；授权分级持久化 |
| **M-P2 托管执行器**（1天） | jev_engine 信号接入 + 七闸风控 + 自动执行（paper/live）+ autopilot_actions 留痕 + 熔断/翻转保护 | 模拟行情跑 20 轮托管决策：闸门拦截正确、执行留痕完整、熔断触发即停 |
| **M-P3 托管中心 UI**（1天） | 移动端托管中心屏 + 任务卡（实时状态）+ 新建 sheet + 授权弹窗 + 暂停/修改/取消 + AI 对话创建托管 | playwright 全路径：授权→新建→运行→持仓出现→暂停→修改→取消 全绿 |
| **M-P4 风控联动巡检**（0.5天） | DeepSeek 巡检识别托管异常 → B 级自动调门控/C 级战略调整 → 熔断告警接入 | 模拟费率翻转→自动平仓+告警；巡检建议→B级自动生效 |
| **M-P5 审计与上线**（0.5天） | admin 托管维度面板 + 全路径自检 + 套餐分档确认 + skill 归档 + 双端提交 | 面板数据真 + 自检全绿 + 提交 |

## 八、风险与边界（诚实声明）

1. **全托管≠保证盈利**：carry 策略本身有费率转负、极端行情风险；AI 托管改善的是执行纪律与决策一致性，不改变策略本身的风险收益特征。
2. **授权的边界**：默认只开放 A 级（交易决策），B 级（门控微调）可选，C 级（战略参数自动改）需特别确认——分级防止「AI 自己改自己的裁判规则」。
3. **成本随规模线性**：每活跃托管用户每 5 分钟一次 Jev 批量调用——规模化时用「无持仓降频 + 决策频率可配」控制。
4. **紧急出口永远在人手里**：全托管总开关一键暂停，任何时候可撤回授权。

## 九、待确认事项

1. 托管默认决策频率：5 分钟（与 Jev 巡检同频）还是 15 分钟（保守省成本）？
2. 默认风控参数：单标的持仓上限 3 个、名义 15U/标的、日损熔断 5U（可改为你定的值）？
3. 授权分级默认开放：A 级 + B 级，C 级关闭——是否符合预期？
4. 取消托管时持仓处理默认：「自动平仓」还是「保留持仓由用户手动处理」？
