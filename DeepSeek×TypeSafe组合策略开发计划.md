# DeepSeek × TypeSafe(Jev) 组合式交易策略开发计划

> 版本 v1.0 · 2026-09-25 · 商业级设计
> 目标：Jev(dev) 做快速监控与执行判断，DeepSeek 做定期巡检与策略调参，两者组合驱动对冲套利与对话式交易执行

---

## 一、白话理解两个模型的分工

| 角色 | 模型 | 特长 | 在系统里的位置 |
|---|---|---|---|
| **执行判断层** | TypeSafe Jev（dev） | 结构化窄决策：是/否、多选、打分，**带校准概率+置信度**，~250ms/次，可并行批量 | 分钟级：盯回测数据+实时行情，回答「现在该开仓吗？该平仓吗？」 |
| **战略调参层** | DeepSeek | 文本推理、长上下文、流式对话 | 小时级：巡检全局，调整 Jev 的决策规则参数（阈值、门控、标的池） |
| **确定性引擎** | 代码（无模型） | 微秒级、可回放、零幻觉 | 毫秒级：下单执行、风控拦截、ATR 止损——**钱永远只由代码动** |

**核心铁律**：模型只做「判断题」，代码只做「执行题」。Jev 输出的是结构化答案（P(开仓)=0.82, confidence=0.9），代码根据答案+门控阈值决定动还是不动。模型永远不直接触碰下单。

---

## 二、三层决策架构

```
┌─────────────────────────────────────────────────────────┐
│ L3 战略层 · DeepSeek（每小时巡检 / 智能触发）              │
│   输入：全市场快照+回测历史+持仓盈亏+费率regime           │
│   输出：策略参数调整建议（θ阈值/置信门控/标的池/风控系数）  │
│   生效：三闸审批（白名单参数→预览→人工批准）→ 写参数文件    │
├─────────────────────────────────────────────────────────┤
│ L2 判断层 · TypeSafe Jev（5分钟巡检 / 事件触发）           │
│   输入：state = 回测指标+实时行情+持仓+费率快照(JSON)      │
│   输出：批量窄决策（noul 开仓时机 / choice 平仓动作 /       │
│         score 风险等级），带 confidence                   │
│   生效：confidence 门控 → 通过才传给 L1；不通过=不动        │
├─────────────────────────────────────────────────────────┤
│ L1 执行层 · 确定性代码（毫秒级，已有）                     │
│   carry 引擎规则 / ATR 风控 / 余额闸 / MIN_ORDER / 审计    │
│   下单、平仓、止损全部由代码执行，可回放可审计              │
└─────────────────────────────────────────────────────────┘
```

**降级链路（永不阻塞下单）**：
- Jev 超时（>2s）→ 跳过 L2，直接走 L1 确定性规则
- confidence < 门控阈值 → 不动作（模型诚实地说「我不确定」= 不动）
- DeepSeek 巡检失败 → 保留现有参数，告警但不影响交易

---

## 三、L2 判断层设计（dev/Jev 的核心职责）

### 3.1 决策问题集（每次批量 fan-out，一次 API 调用全问）

**开仓审批（noul，带 criteria）**
- `open_ok`：给定当前基差/费率/波动率，现在是否值得在该标的开 carry 仓？
  - criteria.true = "费率年化显著高于 θ、基差稳定、无临近结算风险"
  - criteria.false = "费率已回落、波动率异常、临近结算时段"

**持仓动作（choice，5 选项）**
- `position_action`：该持仓下一步动作？持有 / 加仓 / 减仓 / 平仓 / 紧急平仓
  - 返回全概率分布，代码取 argmax + confidence 门控

**风险评分（score，4 级）**
- `risk_level`：当前 regime 风险等级？低 / 正常 / 偏高 / 高危
  - 高危 → L1 风控系数自动收紧（ATR 止损收紧、禁止新开仓）

**标的选择（choice，N 标的）**：当前哪个标的最适合新开仓？（含"都不适合"选项）

### 3.2 状态(state)构造——喂什么

JSON 快照（精简，控制 token 成本）：
- 回测指标：各标的 θ=5 历史轮数/胜率/年化/最大回撤（来自 backtest_results）
- 实时行情：基差 bp、funding 年化、近 1h 波动率、盘口失衡度（来自 orderbook.json）
- 持仓：当前持仓标的/方向/持仓时长/浮盈亏（paper + live）
- 风控：ATR 系数、余额、距结算时间

### 3.3 门控与执行

```
Jev 回答 → conf < 0.6  → 不动作（留痕"模型不确定"）
         → conf ≥ 0.6  → 与 L1 确定性规则合并（AND/OR 模式可配）
         → 动作 → 审计留痕（决策 JSON 全量存盘, 可回放复盘）
```

---

## 四、L3 战略层设计（DeepSeek 巡检）

### 4.1 巡检周期：1 小时固定 + 智能触发

| 触发方式 | 条件 |
|---|---|
| 定时 | 每小时整点（所有付费套餐用户一视同仁，与现有每日巡检报告叠加） |
| 智能触发 | funding 年化 30 分钟内变化 >30%、波动率突增 >2σ、持仓浮亏超阈值、回测指标漂移 |
| 手动 | 用户在 AI 对话中说「现在巡检一遍」 |

### 4.2 巡检输出 → 调参闭环（复用已有三闸）

DeepSeek 巡检报告包含：
1. **市场状态解读**（白话）：费率 regime、波动率、各标的表现
2. **策略参数建议**（结构化 [SUGGEST]）：θ 阈值、Jev 门控 conf 阈值、标的白名单/黑名单、巡检频率、ATR 系数
3. **异常告警**：持仓风险、资金费率翻转预警、回测数据漂移

**生效路径 = 现有三闸**：白名单参数闸（只能改 θ∈(0,20]、conf∈[0.5,0.95]、ATR∈[1,5]、白名单标的）→ 预览闸（卡片展示旧值→新值）→ 人审批准闸（/api/ai/approve）。**DeepSeek 建议 = 建议，人批 = 生效**。

---

## 五、组合式策略：对话式交易执行（用户需求核心）

用户自然语言 → 双模型接力 → 真实执行：

```
用户："BTC 费率 8% 了，帮我开一笔 carry"
  ↓
DeepSeek（意图理解+补全上下文）：识别标的/方向/依据 → 生成执行预案
  ↓
Jev（参数抽取+复核，function_calling 模式）：
   choice(标的) / noul(时机是否合适) / score(风险)
   每个参数带 confidence → 低置信参数回问用户
  ↓
三闸：白名单校验 → 预览卡片（开仓要素+Jev 置信度+预计费率收益）→ 用户点"执行"
  ↓
L1 代码执行 → 成交留痕 → AI 回复白话结果
```

**官方背书**：TypeSafe 官方 cookbook《Function calling》正是这个模式（自然语言交易请求 → 封闭集参数 → confidence 门控 → typed functions），我们按官方 best practice 落地。

**与现有 AI 助手的衔接**：现有 ai_client.py（DeepSeek 流式+工具循环）+ 三闸 + 建议卡片已上线——本计划在其上加「Jev 参数复核层」和「执行类工具」，不推倒重来。

---

## 六、回测数据判断（用户需求核心二）

「dev 用于快速监控回测数据进行判断」落地：

1. **回测指标喂给 Jev**：每日/每小时回测结果（6 双通道标的 θ=5 基线 + 用户指定 θ）自动转成 state 快照
2. **Jev 判断回测**：
   - noul：该标的回测表现是否支持继续纳入策略池（胜率/回撤/样本量综合）
   - choice：回测异常归类（数据问题 / 市场 regime 变化 / 策略失效）
   - score：策略健康度（健康/观察/衰减/失效）
3. **DeepSeek 解释**：Jev 判断结果 + 回测数据 → 白话报告给用户（现有回测总结升级：Jev 定量判断 + DeepSeek 定性解释）
4. **策略衰减预警**：健康度=衰减 → 自动触发 DeepSeek 巡检 → 调参建议 → 人审

---

## 七、商业级工程要求

| 维度 | 方案 |
|---|---|
| **成本控制** | Jev 批量 fan-out（一次调 8-12 问，官方实测 12.2x 便宜）；state 精简 JSON；巡检 1h/次 + 智能触发不滥用；按用户套餐分档用量（free 只读/付费含执行建议） |
| **延迟预算** | Jev 稳态 ~250ms（都柏林实测）→ 5 分钟巡检完全无压力；下单路径不依赖 Jev |
| **安全** | 模型永不直接下单；三闸人审；参数白名单；全量审计留痕（决策 JSON 存盘）；超时降级 L1 |
| **可观测** | 决策留痕文件 + 营收大盘加「AI 决策面板」：Jev 调用次数/置信分布/门控拦截率/与 L1 规则分歧率 |
| **密钥** | 服务器 600 权限文件（沿用 .typesafe_key 机制），gitignore，不进日志不进聊天 |
| **回滚** | 每参数变更一个 commit；一键回退 L3 参数/L2 门控/L1 规则任意层 |

---

## 八、里程碑（估算 4 个工作日）

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M-D1 基建**（0.5天） | typesafe_decision.py 升级：state 构建器（回测+行情+持仓快照）+ Jev 客户端 keep-alive 池 + 延迟/成本日志；新 key 落服务器 | 服务器实测一次批量调用，TTFB/稳态延迟/成本记录 |
| **M-D2 Jev 决策层**（1天） | L2 四类问题集 + conf 门控 + L1 合并 + 超时降级 + 决策留痕；5 分钟巡检线程接入 tick | 模拟场景跑 24 轮决策，留痕可回放；杀 API 测降级不阻塞 |
| **M-D3 巡检调参闭环**（1天） | DeepSeek 1h+智能触发巡检；[SUGGEST] 参数建议 → 三闸 → 参数文件真生效；与每日巡检报告合并 | "改 θ 到 6"建议→批准→strategy_params.json 生效；智能触发实测 |
| **M-D4 对话式执行**（1天） | 执行类工具（开/平 carry 仓）接 Jev 参数复核 + 预览卡 + 人审；AI 助手执行能力上线 | 真机："开一笔 BTC carry"→Jev 置信度显示→批准→paper 成交留痕 |
| **M-D5 bake-off 验证**（0.5天） | Jev vs 确定性基线 vs DeepSeek 三选一：24h 决策样本 ECE 校准 + 门控拦截率 + 分歧分析 | 校准图 + 结论报告：Jev 是否准入资金路径（条件准入，不过就降级为研究工具） |
| **M-D6 上线审计**（0.5天） | AI 决策面板（营收大盘 tab）+ 套餐分档 + 全路径自检 + skill 归档 | 面板数据真 + 全路径 playwright 绿 + 提交 |

---

## 九、风险与边界（商业级诚实声明）

1. **Jev 不是交易圣杯**：它是校准概率决策机，不是预测器。它的价值 = 结构化+置信度+快，不是「更准」。bake-off 不过就只做研究工具，不进资金路径。
2. **回测≠未来**：回测数据喂给 Jev 判断策略健康，但历史表现不保证未来（现有 AI 回测总结已带此风险提示，Jev 判断同样）。
3. **智能触发防抖**：波动率触发要防抖（冷却期），否则震荡市刷爆 API 成本。
4. **用户预期管理**：移动端 UI 明确标注「AI 判断供参考，最终执行需你确认」——托管模式不是全自动（全自动需用户单独开启，默认关闭）。
5. **合规**：对话式执行只对接 paper 与用户自有实盘通道，平台不代持用户资产指令。

---

## 十、待确认事项

1. 新 key 用于替换服务器现有 `.typesafe_key` 还是双 key 并存（测试/生产隔离）？
2. L2 巡检周期默认 5 分钟可接受？（成本：6 标的×4 问≈每 5 分钟 1 次批量调用）
3. 对话式执行首发支持哪些动作？（建议：开仓/平仓/查询，加仓减仓放二期）
