# 全标的实时行情与移动端交易 — 可行性分析与开发计划

> 目标：把 Bybit 全部标的 + Polymarket 全部活跃市场的实时行情接入移动端行情页，并支持按标的交易。
> 状态：可行性已实测验证（2026-09-23），等待确认后实施。

---

## 一、现状盘点（实测）

| 数据面 | 现状 | 差距 |
|---|---|---|
| Bybit 行情桥 | WSS 仅订阅 **BTCUSDT/ETHUSDT** 的 ticker(100ms)+orderbook.200+现货 | 其余 883+ 个线性合约、600+ 现货无数据 |
| Bybit 标的清单 | 无 instruments-info 缓存 | 实测 API 返回 **885 个 linear** 标的（含预上市），每标的含 tickSize/qtyStep/status 精度表 |
| Bybit K线 | 本地 parquet 仅 BTC/ETH（readers.klines 硬限） | 其他标的无 K线 |
| PM 行情 | gamma REST 轮询 → parquet **1 分钟快照**（best_bid/ask/model_p） | 非实时（最长滞后 60s+） |
| PM 实时通道 | 未接入 | **已实测打通**：CLOB WS `wss://ws-subscriptions-clob.polymarket.com/ws/market`，订阅格式 `{"assets_ids":[...],"type":"market","markets":[],"initial_dump":true,"custom_feature_enabled":true}`，实收 `price_change` 事件（含 best_bid/ask）；需 10s PING 心跳 |
| 移动端 /m | 行情页硬编码 2 币 + PM Top3（1m 快照）；交易屏标的选择器仅 2 币 | 无法浏览/选择全市场标的 |
| 交易引擎 | paper_ops/bybit_live 本身 symbol 无关，但依赖价格快照有该标的 spot+perp 价、且无精度/流动性校验 | 新标的缺：精度取整、最小名义、流动性门槛 |
| SSE 管道 | prices/depth 两条 SSE 已就绪（移动端已消费） | 数据源需要扩量 |

**结论：可行。** 两端（Bybit WSS 批量订阅 / PM CLOB WS）均已验证连通，服务器（都柏林）到两交易所延迟 8ms/25-30ms，管道复用现有 SSE 架构。

---

## 二、关键设计决策（需用户确认）

1. **行情全量，交易分级**：
   - 行情：**全部** Bybit linear+spot 标的（~1500）实时价（WSS 批量订阅 tickers，轻量 ~100ms/条）
   - 交易：**Top-N 活跃标的池**（按 24h 成交额排序，默认 Top 50，可配），避免低流动性标的滑点/穿价
2. **盘口按需订阅**：全部标的的 orderbook.200 全量订阅消息量过大（885×3 通道），改为**选中标的动态订阅**（LRU 缓存上限 20 个，1 分钟未用自动退订），服务器收到前端请求才拉对应盘口
3. **PM 实时订阅范围**：按 24h 交易量排序 **Top 100 活跃事件（≈200 个 token）** 订阅 price_change（1s 内价格刷新）；选中市场时按需订阅 book 通道看盘口
4. **精度表缓存**：instruments-info 全量缓存到服务器 JSON（每日刷新），交易/展示统一取 tickSize/qtyStep
5. **兜底策略**：WSS 断线时 REST 轮询兜底（tickers 2s / PM gamma 10s），保证移动端始终有价

---

## 三、分阶段开发计划

### P1 · Bybit 全标的行情（1 天当量）✅ **已完成 2026-09-23**
- 服务器：`bybit_instruments.py`——instruments-info 拉全量（linear+spot）→ 缓存 JSON（status=Trading 过滤 + 精度表 + 成交额排序）
- 桥升级：`bybit_ws_bridge.py` 订阅全部标的 tickers（每消息 10 标的批量订阅，~90 条消息/连接）；`bybit_prices.json` 全量快照（现有 SSE 无需改动即自动携带）
- 后端：`/api/instruments`（标的清单：symbol/名称/分类/24h成交额/精度），`readers.klines` 放开 symbol 限制（parquet 命中 BTC/ETH，其他标的**按需回源 Bybit REST kline 并缓存 60s**）
- 移动端：行情页重做——**搜索框 + 分类 Tab（自选/合约/现货/全部）+ 24h涨跌排序 + 虚拟滚动**（1500 行只渲染可视区）；收藏自选存 localStorage
- **验收**：移动端可搜索到任意 Bybit 标的；Top 200 活跃标的价格 ≤500ms 刷新；点击任意标的进交易屏显示其价格

### P2 · 选中标的实时盘口 + K线（1 天当量）
- 服务器：桥加**动态订阅接口**（内部队列：前端请求 symbol → subscribe orderbook.200 + 现货 ticker → LRU 20 上限；orderbook.json 按标的扩展）
- 移动端：交易屏 K线（任意标的、5分-1日）、盘口（选中标的 1s 内）
- **验收**：移动端选中任意标的 → 盘口 <1s 更新、K线 ≤3s 加载；切换 20 个标的后最旧的自动退订（服务器日志可查）

### P3 · Bybit 全标的交易（1-2 天当量）
- 服务器：`paper_ops` 加**精度取整**（tickSize/qtyStep）+ **最小名义校验**（成交额门槛）+ 标的池白名单（Top-N 可配）；实盘 `bybit_live` 下单前同校验（Bybit 直接按 API 精度下单）
- 移动端：交易屏标的选择器升级为**搜索式抽屉**（首字母/名称搜索，显示价格+24h）；一键对冲/裸腿下单票支持任意标的
- **验收**：Top 50 标的纸面交易全链路（开对冲→持仓→浮盈→平仓）；实盘在任意 Top 50 标的可下单（$6 最小额真实验证，风控链不变）；非白名单标的明确提示「暂不开放交易」

### P4 · PM 全市场实时（1-1.5 天当量）
- 服务器：`pm_clob_ws.py`——CLOB WS 桥：启动拉 gamma 活跃事件 Top 100 → 订阅全部 token 的 price_change（含 best_bid_ask）→ 原子写 `pm_prices.json`（token→{bid,ask,last,ts}）+ 选中市场 book 按需订阅 → 新 SSE `/api/stream/pm`
- 后端：`/api/markets` 报价改为「实时价优先，1m 快照兜底」（merge 层）；`/api/market/{key}` 系列图加实时点
- 移动端：PM 屏市场列表**价格 1s 内实时跳动**；选中市场显示实时盘口（买一/卖一档位）+ 模型价差实时对比
- **验收**：PM 列表价格与 CLOB 官方盘口偏差 =0、滞后 <1s；快照兜底切换无感知

### P5 · 移动端 UI 打磨与联动（1 天当量）
- 行情页与 PM 屏联动：币种页 → PM 对应市场跳转（如 BTC 年底目标价）
- 自选收藏跨屏同步（Bybit+PM 统一自选栏）
- 交易屏：PM 实时盘口下单价格预填（买 YES 预填卖一价）；实盘确认弹窗显示实时价差
- 每屏「?」白话帮助更新（全标的/实时概念）
- **验收**：全站体检（14 页链接+JS 语法）+ 移动端冒烟 + 生产 e2e（新标的交易流）+ 全量回归 88+ 项

---

## 四、风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| Bybit WSS 订阅 885 标的消息量 ~9k/s，服务器 CPU/带宽 | 中 | tickers 仅 3 字段轻量处理；实测压测，超阈值自动降级 Top 300 |
| Bybit WSS/IP 限流（订阅频率） | 中 | 批量订阅 + 指数退避重连；REST 2s 兜底 |
| PM CLOB WS 连接静默断开（需 PING 心跳） | 已实测 | 10s PING + 断线重连 + gamma 快照兜底 |
| 低流动性标的滑点（交易） | 高 | 标的池白名单（成交额门槛）+ 最小名义 + 盘口深度校验 |
| 移动端 1500 行列表卡顿 | 中 | 虚拟滚动 + 只渲染可视区 + 300ms 节流 |
| gamma/CLOB 地理限制 | 低 | 都柏林实测可用；桥跑服务器侧，用户端无感知 |

## 五、交付顺序建议

**P1→P2→P4→P3→P5**：先把「看」打通（全标的行情+PM 实时），再开「交易」白名单。P1+P2+P4 完成后即可演示「全市场实时行情」，P3 交易分级随后。

预计总量 **5-7 个工作日当量**，每阶段独立可验收、独立可回滚（桥/后端/前端分层提交）。
