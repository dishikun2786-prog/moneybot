# 托管模式 × 手动模式（双模式交易台）开发计划

> 2026-09-22 · 全部纸面交易 · 针对两个策略**分别独立切换**
> 背景：目前两引擎均为"托管"（模型全自动交易），手动操作已具备（手动交易功能已上线），
> 本计划补齐「手动模式」——暂停模型自动决策，由用户全权决策，并可随时切回托管。

## 一、需求定义

| 模式 | 现货×永续套利 (carry) | 预测市场对冲 (paper_pm) |
|---|---|---|
| **托管模式**（默认） | 引擎全自动：窗口入场/单边平仓/孤儿复用 | 引擎全自动：edge 开平仓 |
| **手动模式** | 引擎自动交易**全部暂停**；用户用已上线的手动操作决策（一键对冲/平合约腿/平现货转裸仓/平孤儿/全平）；裸腿 TP/SL 仍执行（用户指令） | 引擎自动交易**暂停**；用户手动开仓（**新增**）+ 手动平仓（已有） |

**关键语义**：
- 两策略模式**互相独立**（carry 托管 + PM 手动 可共存）
- 模式切换**下个 tick 生效**（≤60s，热读 mode.json）
- 手动模式下**持续有效的**：funding 结算记账、裸腿止盈止损（用户下的单）、数据采集、风控熔断（HALT）
- 手动模式下**暂停的**：模型自动入场、模型自动平仓、孤儿腿自动复用/处置（交给用户）

## 二、架构设计

### 2.1 模式状态
```
~/polymarket/engine/mode.json = {"carry": "auto", "paper_pm": "auto"}
```
- 独立于 strategy_params.json（参数=模型行为；模式=运营状态）
- 引擎每 tick 热读；切换接口写文件 + 审计
- 切换留痕：logs/mode_actions.jsonl（时间/策略/旧模式/新模式）

### 2.2 引擎改造（托管/手动分支）
```
carry_engine.cycle():
  mode = load_mode()["carry"]
  若 auto: 现有全部逻辑 (裸腿TP/SL + 孤儿处置 + 持仓管理 + 开新仓)
  若 manual: 仅保留 [裸腿TP/SL检查 + funding结算 + 跨天滚动 + 打印"(手动模式)"]
paper_engine.cycle():
  若 manual: 跳过持仓管理与入场, 仅打印状态
```

### 2.3 API
- `GET /api/mode` → {"carry": "...", "paper_pm": "..."}
- `POST /api/mode` `{strategy, mode}` → 校验(mode∈auto/manual) → 写文件 + 审计 → 返回

### 2.4 新增：PM 手动开仓（paper_ops.open_pm）
- 入参：market key + side(BUY/SELL) + size_usd
- 定价：点击时刻该市场最新盘口快照（ask 买入 / bid 卖出，与引擎同源）
- 校验：市场存在/盘口有效/金额 1-20$；写 paper_state + 成交带 `MANUAL_OPEN_PM` + 审计
- 手动开仓的 PM 仓位在手动模式下不受引擎平仓逻辑影响；切回托管后引擎接管（按 edge 规则退出）

### 2.5 交易室 UI
| 元素 | 设计 |
|---|---|
| 模式切换器（持仓面板顶部） | 两个策略各一组「🤖托管 / 🖐手动」胶囊开关 + 状态徽标（托管中=绿 / 手动模式=橙） |
| 切换确认弹窗 | 说明该模式行为差异（手动模式=模型暂停，自己决策；风险提示） |
| 手动模式横幅 | 面板顶部警示条：「现货套利处于手动模式，自动交易已暂停」 |
| PM 手动开仓票 | 市场选择器（模型价/盘口价/价差 edge 列表，复用 /api/markets）+ 方向买入/卖出 + 金额 → 开仓 |
| 移动端 | 模式开关胶囊 ≥44px 触控；横幅/开仓票全宽 |

## 三、实施里程碑

### M1 — 模式基础设施（~120 行）
- `mode.py`（读/写/校验/审计）
- carry_engine / paper_engine 手动分支
- `/api/mode` GET/POST + main.py 白名单
- 单元测试：mode 读写、引擎手动分支不发单、切换审计

### M2 — PM 手动开仓（~70 行）
- `paper_ops.open_pm` + 成交带/审计翻译
- 单元测试：定价/校验/记账

### M3 — 交易室双模式 UI（~220 行）
- 模式切换器 + 徽标 + 确认弹窗 + 手动横幅
- PM 开仓票（市场列表/方向/金额/确认）
- 手动模式下持仓按钮全可用（已上线的手动操作）

### M4 — 移动端 + 端到端验收
- 375px 布局验证 + 触控
- e2e：切换→引擎行为断言（手动模式 60s 内零自动成交）→ 手动操作正常 → 切回托管自动恢复
- 真实状态测试沿用备份/恢复模式

## 四、安全与边界

| 事项 | 处理 |
|---|---|
| 手动模式裸仓风险 | 裸腿强制 TP/SL 已上线，手动模式仍执行 |
| 手动模式持仓无人管 | UI 横幅持续提示 + 持仓卡片操作按钮常驻 |
| 切换瞬间竞态 | mode.json 原子写 + 引擎 tick 热读（下轮生效） |
| PM 手动开仓无模型 edge 提示 | 开仓票展示模型价 vs 盘口价价差（用户自主判断，不做阻断） |
| 托管模式行为不变 | 默认 auto；所有现有自动逻辑零改动（回归验证） |

## 五、交付物与验收
- 交付：mode.py / paper_ops.open_pm / 引擎双分支 / API 2 端点 / 交易室模式 UI + PM 开仓票 / 移动端适配 / 单元测试 ~30 项 / 模式切换审计
- 验收标准：
  1. 两策略独立切换且互不影响
  2. 手动模式 60s 观察窗口零自动成交；funding 记账与裸腿 TP/SL 仍工作
  3. PM 手动开仓→持仓出现→手动平仓闭环；切回托管后引擎接管
  4. 切换全留痕；375px 全可操作
- 预估 ~450 行，2 轮完成
